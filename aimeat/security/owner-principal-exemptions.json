{
  "note": "Doors that decide a refusal by comparing the owner NAME on the verified token, with nothing on the door naming the principal. Triaged 2026-09-04 entry by entry against the source and the door's middleware chain, on the day the gate was written. `auth.owner` reads identically on an owner session, an agent JWT, an ecosystem token, a PAT and an app grant, so the comparison refuses a different PERSON and admits anything acting in this person's name. Invariant 11 says that is the right question for a person's own DATA and the wrong one for their ACCOUNT. The prefixes mean different things: OWNER SESSION ONLY — the chain already excludes machine principals, so the comparison is a data-ownership check; ACTS FOR THE OWNER — a scoped machine principal is meant to reach this, on the owner's own records; A PUBLIC READ and NOT A DOOR — what they say. Three entries were raised as open questions on 2026-09-04 (an agent's key, an agent's scopes, an ecosystem app's approval) because an owner-level PAT satisfies requireRole('owner') on doors that GRANT something. Decided the same day: a PAT is how the account is managed from outside a browser, so the owner's own long-lived token is the owner. requireOwnerPrincipal() would not have changed it either — a PAT satisfies that too, and test/e2e-account-security-gate.ts phase 3b now asserts a PAT reaching all twelve account doors so tightening it fails a test rather than somebody's automation. One door found by this gate was not exempted but fixed: GET /v1/ghii/:ghii/credential, which minted a signed identity credential for anything holding the account name (verification.ts v2.2.0). Invariant 11, docs/coding-guidelines/security-development-dna.md.",
  "exempt": {
    "src/routes/a2a.ts:a2aRouter": "ACTS FOR THE OWNER — the comparison IS this road's fence and the handler says so: the A2A interface carries work between principals of ONE account, and any of that account's principals may use it. Per-method scope checks live inside the JSON-RPC dispatch because the gate is per method, not per HTTP door. A principal test here would refuse the agents the road exists for.",
    "src/routes/agents/device-auth.ts:POST /v1/agents/verify": "NOT A SESSION — the device-authorization verify step, unauthenticated by protocol (RFC 8628). The name it compares is the one recorded on the pending authorization, not one taken from a caller's token, so there is no principal to name yet: this is where one is being established.",
    "src/routes/agents/management.ts:POST /v1/agents/:gaii/export": "OWNER SESSION ONLY — requireAuth + requireRole('owner'), which an agent or ecosystem token never satisfies (they carry roles ['agent'] / ['ecosystem']). The comparison narrows to the owner's own agent records, which is data ownership and what a name is for.",
    "src/routes/agents/management.ts:POST /v1/agents/import": "OWNER SESSION ONLY — requireRole('owner'). The comparison refuses an import whose payload names a different owner, so it reads the record being written rather than the caller.",
    "src/routes/agents/management.ts:POST /v1/agents/:gaii/rekey": "OWNER SESSION ONLY, PAT INCLUDED ON PURPOSE — requireRole('owner') keeps agents and ecosystem tokens out. An owner-level personal access token also satisfies it, and that is the point of a PAT: it is how the account is managed from outside a browser (developer's decision, 2026-09-04). requireOwnerPrincipal() would change nothing here, because a PAT satisfies that too — resolvePatToken builds the same VerifiedToken and maybeSetPatBrowserSession deliberately turns an owner PAT into a browser session. Asserted in test/e2e-account-security-gate.ts phase 3b so tightening it fails a test instead of breaking automation.",
    "src/routes/agents/management.ts:POST /v1/agents/:gaii/port": "OWNER SESSION ONLY — requireRole('owner'); moving one's own agent to another node, narrowed to the owner's own agents by the comparison.",
    "src/routes/agents/management.ts:PATCH /v1/agents/:name/scopes": "OWNER SESSION ONLY, PAT INCLUDED ON PURPOSE — requireRole('owner') keeps machine principals out. This door grants permissions, which is the act invariant 11 is most about, and the answer is that the owner's own long-lived token is the owner: a PAT exists so everything can be managed (developer's decision, 2026-09-04). Same as /rekey above.",
    "src/routes/agents/management.ts:PATCH /v1/agents/:name/federate": "OWNER SESSION ONLY — requireRole('owner'). Turns an agent's federation reachability on or off, on the owner's own agent.",
    "src/routes/agents/management.ts:PUT /v1/agents/:name/cors": "OWNER SESSION ONLY — requireRole('owner'). Sets which web origins may reach the owner's own agent.",
    "src/routes/agents/profile-metadata.ts:GET /v1/agents/:name/engagements": "OWNER SESSION ONLY — requireRole('owner'), reading the owner's own agent's engagements. Data, and the name is the right question.",
    "src/routes/agents/profile-metadata.ts:PATCH /v1/agents/:name/max-concurrent-tasks": "OWNER SESSION ONLY — requireRole('owner'), a pacing field on the owner's own agent.",
    "src/routes/agents/profile-metadata.ts:PATCH /v1/agents/:name/schedule-constraints": "OWNER SESSION ONLY — requireRole('owner'), a scheduling field on the owner's own agent.",
    "src/routes/agents/registration.ts:POST /v1/agents": "OWNER SESSION ONLY — requireAuth + requireLocalSession + requireRole('owner'). The comparison refuses a body naming an owner other than the caller, which is a cross-owner check on the record being created rather than a principal question.",
    "src/routes/agui.ts:POST /v1/agui/:owner/:agent": "ACTS FOR THE OWNER — requireScope gates the door and the comparison keeps the caller inside its own account's agents. An agent driving its owner's agent UI is the designed model.",
    "src/routes/agui.ts:GET /v1/agui/:owner/:agent/:taskId": "ACTS FOR THE OWNER — the read beside the write above, on the owner's own agent's task. Carries requireAuth alone rather than a scope, which is check:route-scopes' question and not this one.",
    "src/routes/chat-instances.ts:GET /v1/chat-instances/:id": "ACTS FOR THE OWNER — the caller's own chat instance, or an operator. Reading one's own conversation is data, and every principal that reaches it is already acting for that person.",
    "src/routes/chat-instances.ts:PUT /v1/chat-instances/:id": "ACTS FOR THE OWNER — updating the caller's own chat instance, or an operator. Same reasoning as the read above.",
    "src/routes/chat-instances.ts:DELETE /v1/chat-instances/:id": "ACTS FOR THE OWNER — deleting the caller's own chat instance, or an operator. It ends a conversation record, not a credential, so a name is the right question.",
    "src/routes/cortex.ts:PUT /v1/cortex/:name": "ACTS FOR THE OWNER — requireScope on the door, and the comparison is against `installedBy`, so a caller may only replace a pack it (or its owner) installed. An operator also passes, deliberately.",
    "src/routes/cortex.ts:POST /v1/cortex/:name/visibility": "ACTS FOR THE OWNER — requireScope, and `installedBy` again. Publishing one's own pack more widely is the owner's call and an agent may make it for them.",
    "src/routes/cortex.ts:GET /v1/cortex/:name/export": "ACTS FOR THE OWNER — requireScope, `installedBy`. Reading out a pack the caller's account installed.",
    "src/routes/ecosystem-apps.ts:POST /v1/ecosystem-apps/:userCode/approve": "OWNER SESSION ONLY, PAT INCLUDED ON PURPOSE — this is CONSENT: approving it hands an outside app a token, a scope list and a data area under this person's name. requireRole('owner') keeps agents and ecosystem tokens out, and an owner-level PAT gets through because the owner's own token is the owner (developer's decision, 2026-09-04). Raised as the sharpest of the three because what it grants is access for somebody else, and decided the same way.",
    "src/routes/exchange-market.ts:GET /v1/exchange/offerings/:id/consumers": "ACTS FOR THE OWNER — who is buying the caller's own offering. The comparison answers 404 rather than 403 on purpose, so the existence of another owner's offering is not disclosed.",
    "src/routes/exchange-market.ts:DELETE /v1/exchange/offerings/:id": "ACTS FOR THE OWNER — withdrawing the caller's own market listing, 404 on anyone else's for the same non-disclosure reason.",
    "src/routes/exchange-market.ts:POST /v1/exchange/needs/:id/close": "ACTS FOR THE OWNER — closing the caller's own posted need.",
    "src/routes/organisms/shared.ts:requireWsManager": "NOT A DOOR — a shared middleware helper, and the comparison is only one arm of its test: the workspace's creator OR an organism creator OR an admin. Membership and role in the organism are what it actually decides on, and the name arm is the 'this is mine' case.",
    "src/routes/organisms/workspace-ops.ts:POST /v1/organisms/:id/workspace/engagements": "ACTS FOR THE OWNER — the comparison parses the GAII in the request and refuses one belonging to a different owner, so it is a cross-owner check on the argument rather than a question about the caller.",
    "src/routes/packages.ts:GET /v1/packages/:groupId/versions/:version": "A PUBLIC READ — no auth on the door at all; the comparison exists so a PRIVATE package answers 404 to everyone but its author. `req.auth?.owner` is read optionally for exactly that reason, and naming a principal would mean requiring one, which this door does not.",
    "src/routes/packages.ts:GET /v1/packages/:groupId/export": "A PUBLIC READ — same shape as the version read above: private packages stay 404 to everyone but the author.",
    "src/routes/packages.ts:GET /v1/packages/:groupId": "A PUBLIC READ — same shape again, on the package record itself.",
    "src/routes/packages.ts:GET /v1/packages/:groupId/upstream-check": "PROTECTS DATA, NOT THE ACCOUNT - it reads one package record the caller authored and asks the node it came from whether there is a newer version. Nothing about the account changes, and every principal that reaches it is already acting for that person under `packages:write`. Same reasoning as the package reads listed above."
  }
}
