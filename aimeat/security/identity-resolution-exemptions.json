{
  "note": "Routes and services that read `sub` off the verified token and never call resolveIdentity(). Seeded 2026-09-04 from the state of that day and NOT TRIAGED: an entry here is a question, not a clearance. Reading `sub` is correct when the unit means the bare ACCOUNT name (a path segment to compare against, a log line) and wrong when it means the identity a record is filed under, and only a person reading the unit can say which. Triaging one means deleting the entry or replacing this sentence with what makes it right.",
  "exempt": {
    "src/routes/a2a.ts:a2aRouter": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/admin-economy.ts:POST /v1/admin/mint": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/admin-extensions.ts:POST /v1/admin/extensions/available/:name/install": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agents-v2/basic-agents.ts:POST /v1/agents/v2/basic-agents/request": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agents-v2/enrolment.ts:POST /v1/agents/v2/enrol": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/apps/read.ts:GET /v1/apps/:owner/:filename": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/auth.ts:POST /v1/auth/refresh": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/cortex.ts:GET /v1/cortex/:name/prompts/:promptName": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/disputes.ts:POST /v1/work/:tc/dispute": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/disputes.ts:POST /v1/work/:tc/counter-dispute": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/disputes.ts:POST /v1/work/:tc/redeliver": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/disputes.ts:POST /v1/work/:tc/accept-fault": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/disputes.ts:POST /v1/work/:tc/offer-partial": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/disputes.ts:POST /v1/work/:tc/accept-redelivery": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/disputes.ts:POST /v1/work/:tc/accept-partial": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/disputes.ts:POST /v1/work/:tc/reject-partial": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/disputes.ts:POST /v1/work/:tc/withdraw-dispute": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/disputes.ts:POST /v1/admin/disputes/:id/rule": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/extensions/crud.ts:POST /v1/extensions/:name/activate": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/flags.ts:POST /v1/flags": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/instances/install.ts:POST /v1/packages/:groupId/install": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/instances/manage.ts:GET /v1/instances/:id/status": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/instances/manage.ts:DELETE /v1/instances/:id": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/instances/migration.ts:POST /v1/instances/:id/migration-prompt": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/instances/migration.ts:POST /v1/instances/:id/apply-migration": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/invoke.ts:POST /v1/invoke": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/memory/crud.ts:POST /v1/memory": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/memory/files.ts:GET /v1/memory/files": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/memory/key.ts:GET /v1/memory/:key": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/memory/key.ts:PUT /v1/memory/:key": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/organisms/shared.ts:memberRole": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/packages.ts:POST /v1/packages/import": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/packages.ts:POST /v1/packages": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/packages.ts:POST /v1/packages/:groupId/propose": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/prompts.ts:GET /v1/agents/me/handbook/:module": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/prompts.ts:GET /v1/agents/me/handbook": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/site-layout.ts:PUT /v1/site/layout/:surface": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/site-layout.ts:DELETE /v1/site/layout/:surface": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/site-layout.ts:POST /v1/site/layout/:surface/reset": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/site-layout.ts:POST /v1/site/layout/:surface/restore": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/site-layout.ts:POST /v1/site/layout-import": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/site-layout.ts:POST /v1/site/freeform": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/site.ts:POST /v1/site/template": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/site.ts:DELETE /v1/site/template": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/site.ts:POST /v1/site/import": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/site.ts:POST /v1/site/cache-invalidate": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/site.ts:PUT /v1/site/header-nav": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/site.ts:POST /v1/site/memory": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/site.ts:DELETE /v1/site/memory/:key": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/skills.ts:POST /v1/agents/:name/skills": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/templates.ts:POST /v1/templates": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/templates.ts:POST /v1/templates/:id/approve": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/templates.ts:POST /v1/templates/:id/reject": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/templates.ts:POST /v1/templates/:id/suspend": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/templates.ts:POST /v1/templates/:id/review": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/templates.ts:POST /v1/templates/:id/discussion": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/work.ts:POST /v1/work/request": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/work.ts:POST /v1/work": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/work.ts:POST /v1/work/batch": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/work.ts:GET /v1/work/inbox": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/work.ts:GET /v1/work/sent": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/work.ts:GET /v1/work/overview": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/work.ts:POST /v1/work/:tc/accept": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/work.ts:POST /v1/work/:tc/deliver": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/services/connect-tunnel.ts:handleAttach": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/actions.ts:POST /v1/actions": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/actions.ts:DELETE /v1/actions/:id": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/actions.ts:PUT /v1/actions/:id": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/admin-extensions.ts:POST /v1/admin/extensions/scaffold": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/admin-extensions.ts:PUT /v1/admin/extensions/available/:name/scripts/:actionId": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/admin-extensions.ts:POST /v1/admin/extensions/available/:name/actions": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/admin-extensions.ts:POST /v1/admin/extensions/available/:name/reinstall": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/admin-maintenance.ts:POST /v1/admin/maintenance": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/admin-organisms.ts:POST /v1/admin/organisms/:id/ownership": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/admin.ts:POST /v1/admin/seed-examples": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agent-activity.ts:canAccess": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agent-capabilities.ts:canAccess": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agent-directives.ts:canAccessAgent": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agent-integration.ts:canAccessAgent": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agent-messages.ts:canAccessAgent": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agent-messages.ts:POST /v1/agents/:name/messages": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agent-messages.ts:PATCH /v1/agents/:name/messages/:id": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agent-onboarding.ts:canAccessAgent": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agent-skill-bundle.ts:resolveAndAuthorize": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agent-tasks.ts:canAccessTask": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agent-tasks/completion.ts:POST /v1/agents/:name/tasks/:id/rate": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agent-tasks/create-read.ts:POST /v1/agents/:name/tasks": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agent-tasks/create-read.ts:GET /v1/agents/:name/tasks": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agent-tasks/create-read.ts:GET /v1/agents/:name/tasks/:id": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agent-tasks/lifecycle.ts:PATCH /v1/agents/:name/tasks/:id": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agent-telemetry.ts:canAccessAgent": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agent-webhook.ts:canAccessAgent": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agents-v2/agent-proposals.ts:POST /v1/agents/v2/agent-proposals": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agents-v2/card.ts:POST /v1/agents/:gaii/card": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agents/device-auth.ts:POST /v1/agents/device-authorize": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agents/management.ts:DELETE /v1/agents/:name": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agents/profile-metadata.ts:POST /v1/checkin": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agui.ts:POST /v1/agui/:owner/:agent": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/agui.ts:GET /v1/agui/:owner/:agent/:taskId": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/appeals.ts:POST /v1/flags/:flagId/appeal": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/appeals.ts:POST /v1/appeals/:id/review": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/auth-otk.ts:POST /v1/auth/connectivity-key": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/cortex.ts:callerOf": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/cortex.ts:PUT /v1/cortex/:name": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/disputes.ts:GET /v1/work/:tc/dispute": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/disputes.ts:POST /v1/work/:tc/escalate": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/ecosystem-events.ts:POST /v1/ecosystem/events": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/extensions/crud.ts:PUT /v1/extensions/:name": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/extensions/crud.ts:PATCH /v1/extensions/:name/actions/:actionId": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/extensions/crud.ts:DELETE /v1/extensions/:name": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/extensions/crud.ts:POST /v1/extensions/:name/deactivate": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/extensions/instances.ts:POST /v1/extensions/:name/instances": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/extensions/instances.ts:PATCH /v1/extensions/:name/instances/:instanceId": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/extensions/instances.ts:DELETE /v1/extensions/:name/instances/:instanceId": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/federation-sync/routing.ts:POST /v1/federation/route": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/federation-sync/routing.ts:POST /v1/federation/cross-node/work": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/flags.ts:PUT /v1/flags/:id": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/knowledge/packages-core.ts:GET /v1/knowledge/:id": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/memory/bulk.ts:GET /v1/memory/export": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/memory/crud.ts:GET /v1/memory": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/memory/crud.ts:GET /v1/memory/search": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/memory/key.ts:GET /v1/memory/:gaii/:key": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/notifications.ts:POST /v1/notifications": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/organisms/workspace-documents.ts:callerOf": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/prompts.ts:GET /v1/agents/me/handbook/offerings": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/prompts.ts:GET /v1/prompts/draft-offer": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/prompts.ts:GET /v1/prompts/:tier": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/realtime.ts:POST /v1/realtime/rooms": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/realtime.ts:DELETE /v1/realtime/rooms/:roomId": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/schemas.ts:PUT /v1/memory/:key/schema": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/schemas.ts:DELETE /v1/memory/:key/schema": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/skills.ts:POST /v1/skills": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/skills.ts:GET /v1/agents/:name/skills": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/subdomains.ts:GET /": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/subdomains.ts:GET /:owner/:filename": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/wallet.ts:GET /v1/wallet": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/work.ts:GET /v1/work/:tc": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/work.ts:POST /v1/work/:tc/progress": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/work.ts:POST /v1/work/:tc/reject": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/routes/work.ts:POST /v1/work/:tc/rate": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed.",
    "src/services/connect-tunnel.ts:handleConnection": "SEEDED 2026-09-04, NOT TRIAGED — reads `sub` and never calls resolveIdentity. Whether that is the account name it wants or the identity it should have resolved is a question nobody has answered. Kept so the gate can refuse a NEW one; the decision is still owed."
  }
}
