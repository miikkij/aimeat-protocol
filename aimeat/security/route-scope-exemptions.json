{
  "note": "Routes with no explicit authorization middleware. Triaged 2026-08-16 against the source, entry by entry, with a second reader trying to refute every clearance: 44 open by design, 108 decided somewhere the scanner cannot look, 182 real debt (each names the scope word that fits and what an app-grant token gets today), 5 removed because the route now carries a gate. A DEBT line is not an approval: it is a hole with a name, kept here so the ratchet stays green while the work is scheduled. Full triage and the decision it needs: docs/internal/route-scope-exemption-triage.md",
  "exempt": {
    "src/routes/access-tokens.ts:POST:/v1/auth/token/exchange": "OPEN BY DESIGN — The presented Personal Access Token IS the credential, so no session exists here by construction; resolvePat verifies hash, expiry and the owner's current roles, and the login rate limit bounds guessing. [triaged 2026-08-16]",
    "src/routes/admin.ts:POST:/v1/admin/setup/auth": "OPEN BY DESIGN — This is the admin-password login itself, so no session can exist yet; the password is compared timing-safely and the door is rate limited by adminAuthRateLimit. [triaged 2026-08-16]",
    "src/routes/admin.ts:POST:/v1/admin/setup/register": "GATED IN THE HANDLER OR THE SERVICE — The bootstrap door for a node with no accounts yet: authorized inside the handler (admin.ts:143-148) by the admin session cookie or the timing-safe admin password, which is why every owner it creates is granted the operator role. [triaged 2026-08-16]",
    "src/routes/admin.ts:POST:/v1/admin/setup/token": "GATED IN THE HANDLER OR THE SERVICE — Two proofs inside the handler (admin.ts:231-236 and 250-260): the admin password or session, plus possession of the owner's private key checked against the stored public key before any JWT is issued. [triaged 2026-08-16]",
    "src/routes/admin.ts:POST:/v1/admin/setup/initial-otk": "GATED IN THE HANDLER OR THE SERVICE — Authorized inside the handler (admin.ts:281-286) by the admin session cookie or the timing-safe admin password, rate limited by adminAuthRateLimit; it mints a long-lived OTK for any named owner, so that password is the whole of the decision. [triaged 2026-08-16]",
    "src/routes/admin.ts:POST:/v1/admin/seed-examples": "GATED IN THE HANDLER OR THE SERVICE — Authorized inside the handler (admin.ts:612-622): an operator JWT, or the node admin password / admin session cookie verified timing-safely. The JWT is never trusted for anything below the operator role. [triaged 2026-08-16]",
    "src/routes/agent-activity.ts:GET:/v1/agents/:name/activity/log": "GATED IN THE HANDLER OR THE SERVICE — canAccess() at agent-activity.ts:46-50 admits only the owner session or the agent itself, and the GAII it compares against is derived from the caller's own owner, so the task-event drill-down cannot be read sideways. [triaged 2026-08-16]",
    "src/routes/agent-activity.ts:GET:/v1/agents/:name/activity/overview": "GATED IN THE HANDLER OR THE SERVICE — The subtab composite carries the same owner-or-self decision as the five reads it folds: canAccess() at agent-activity.ts:46-50, on a GAII built from the caller's own owner. [triaged 2026-08-16]",
    "src/routes/agent-activity.ts:GET:/v1/agents/:name/activity": "GATED IN THE HANDLER OR THE SERVICE — Owner-or-self is decided in the handler by canAccess() (agent-activity.ts:46-50) against an agent GAII built from the token's own owner, so an app grant and a sibling agent are both refused and no principal can name another owner's agent. [triaged 2026-08-16]",
    "src/routes/agent-activity.ts:GET:/v1/agents/:name/statistics": "GATED IN THE HANDLER OR THE SERVICE — canAccess() at agent-activity.ts:46-50 restricts it to the owner session or the agent itself; the rollups are recomputed from the tasks, so the response cannot be forged even by a caller that passes. [triaged 2026-08-16]",
    "src/routes/agent-activity.ts:GET:/v1/agents/:name/quality/overview": "GATED IN THE HANDLER OR THE SERVICE — Owner-or-self via canAccess() (agent-activity.ts:46-50). It is a GET that also refreshes the public statistics cache, so the same gate is what keeps a sibling agent from triggering a rewrite of another agent's published rollup. [triaged 2026-08-16]",
    "src/routes/agent-capabilities.ts:PUT:/v1/agents/:name/capabilities": "GATED IN THE HANDLER OR THE SERVICE — A mutation, but owner-or-self is decided in the handler by canAccess() (agent-capabilities.ts:38-44); the MCP twin aimeat_agent_capabilities_report is exempted on the same reasoning (src/mcp/catalog/scopes.ts:80), so both surfaces agree that an agent describing itself needs no extra word. [triaged 2026-08-16]",
    "src/routes/agent-capabilities.ts:GET:/v1/agents/:name/capabilities": "GATED IN THE HANDLER OR THE SERVICE — canAccess() at agent-capabilities.ts:38-44 admits the owner session or the agent itself only, on a GAII built from the caller's own owner. [triaged 2026-08-16]",
    "src/routes/agent-directives.ts:GET:/v1/agents/:name/data-access/overview": "GATED IN THE HANDLER OR THE SERVICE — Owner-or-self via canAccessAgent() (agent-directives.ts:82-85), which is what keeps the memory-key listing this composite returns from being read by a sibling principal. [triaged 2026-08-16]",
    "src/routes/agent-directives.ts:GET:/v1/agents/:name/directives": "DEBT medium, wants task:read — 200 with the full directives record. An app grant holding any single scope reaches it: requireAuth() passes, the agent GAII is built from req.auth!.owner, and no check follows — so the app reads the owner's rules, the memory key prefixes an agent is pointed at, its shared tags and its token budget… [triaged 2026-08-16]",
    "src/routes/agent-integration.ts:GET:/v1/agents/:name/inbox": "GATED IN THE HANDLER OR THE SERVICE — canAccessAgent() at agent-integration.ts:58-64 admits the owner session or the named agent itself, on a GAII derived from the caller's own owner. [triaged 2026-08-16]",
    "src/routes/agent-integration.ts:GET:/v1/agents/:name/integration-kit": "GATED IN THE HANDLER OR THE SERVICE — Owner-or-self via canAccessAgent() (agent-integration.ts:58-64); the kit names the agent's own endpoints and its owner-configured max_concurrent_tasks, so it is the agent's own configuration rather than a public document. [triaged 2026-08-16]",
    "src/routes/agent-integration.ts:GET:/v1/agents/:name/tasks/wait": "GATED IN THE HANDLER OR THE SERVICE — canAccessAgent() at agent-integration.ts:58-64 decides it before the poll starts, and every read inside the loop is keyed to the GAII that check resolved. [triaged 2026-08-16]",
    "src/routes/agent-messages.ts:POST:/v1/agents/:name/messages": "GATED IN THE HANDLER OR THE SERVICE — canAccessAgent() at agent-messages.ts:85-95 is deny-by-default, and the sender identity is read from the session rather than the request, so a message cannot be written in another principal's name. [triaged 2026-08-16]",
    "src/routes/agent-messages.ts:GET:/v1/agents/:name/messages/inbox": "GATED IN THE HANDLER OR THE SERVICE — canAccessAgent() at agent-messages.ts:85-95 is deny-by-default and admits only the owner session or the named agent, so an agent's inbox is not readable sideways. [triaged 2026-08-16]",
    "src/routes/agent-messages.ts:GET:/v1/agents/:name/messages/overview": "GATED IN THE HANDLER OR THE SERVICE — The mount composite carries the same owner-or-self decision as the three reads it folds: canAccessAgent() at agent-messages.ts:85-95. [triaged 2026-08-16]",
    "src/routes/agent-messages.ts:GET:/v1/agents/:name/messages/threads": "GATED IN THE HANDLER OR THE SERVICE — canAccessAgent() at agent-messages.ts:85-95 admits the owner session or the named agent only; the linked task titles it resolves are the caller's own tasks for the same reason. [triaged 2026-08-16]",
    "src/routes/agent-messages.ts:GET:/v1/agents/:name/messages": "GATED IN THE HANDLER OR THE SERVICE — canAccessAgent() at agent-messages.ts:85-95 ends in `return false`, so only an owner session or the named agent itself reads the history; every other principal class is refused by default. [triaged 2026-08-16]",
    "src/routes/agent-messages.ts:PATCH:/v1/agents/:name/messages/:id": "GATED IN THE HANDLER OR THE SERVICE — Two decisions, both in the handler: canAccessAgent() (85-95) answers who may act as this agent, and the message row itself is authorized against resolveIdentity at 266-274 so the client-supplied id cannot name someone else's message. [triaged 2026-08-16]",
    "src/routes/agent-onboarding.ts:GET:/v1/agents/:name/onboarding": "GATED IN THE HANDLER OR THE SERVICE — Owner-or-self via canAccessAgent() (agent-onboarding.ts:121-126); the response carries the agent's step details, its test task id and its post-onboarding checklist, all read against that same resolved GAII. [triaged 2026-08-16]",
    "src/routes/agent-onboarding.ts:POST:/v1/agents/:name/onboarding/step/:id": "GATED IN THE HANDLER OR THE SERVICE — Owner-or-self via canAccessAgent() (agent-onboarding.ts:121-126). Confirming a step is the agent reporting its own progress, which is the written reason its four MCP twins sit in SCOPE_EXEMPT_TOOLS (src/mcp/catalog/scopes.ts:85-92); starting and cancelling onboarding are the different act and carry… [triaged 2026-08-16]",
    "src/routes/agent-skill-bundle.ts:GET:/v1/agents/:name/skill-bundle": "GATED IN THE HANDLER OR THE SERVICE — resolveAndAuthorize() at agent-skill-bundle.ts:69-78 is deny-by-default and returns the GAII only for the owner session or the agent itself, which matters because the ZIP embeds the merged system, owner and agent directives plus the agent's webhook URL. [triaged 2026-08-16]",
    "src/routes/agent-skill-bundle.ts:GET:/v1/agents/:name/skill-bundle/version": "GATED IN THE HANDLER OR THE SERVICE — Same deny-by-default resolveAndAuthorize() as the download (agent-skill-bundle.ts:69-78); the version is derived from the same generated bundle, so the two doors cannot disagree about who may look. [triaged 2026-08-16]",
    "src/routes/agent-tasks/completion.ts:POST:/v1/agents/:name/tasks/:id/event": "GATED IN THE HANDLER OR THE SERVICE — canAccessTask() (src/routes/agent-tasks.ts:104-112) authorizes against the loaded task, so an event can only be appended to a task the caller owns or is the agent for. [triaged 2026-08-16]",
    "src/routes/agent-tasks/completion.ts:POST:/v1/agents/:name/tasks/:id/complete": "GATED IN THE HANDLER OR THE SERVICE — canAccessTask() (src/routes/agent-tasks.ts:104-112) decides it on the loaded task record, so only the task's owner or the agent that holds it can hand the work in, and the provenance stamp is taken from resolveIdentity rather than the body. [triaged 2026-08-16]",
    "src/routes/agent-tasks/completion.ts:POST:/v1/agents/:name/tasks/:id/fail": "GATED IN THE HANDLER OR THE SERVICE — canAccessTask() (src/routes/agent-tasks.ts:104-112) decides it on the task record itself, so only the task's owner or its agent can mark the work failed. [triaged 2026-08-16]",
    "src/routes/agent-tasks/completion.ts:POST:/v1/agents/:name/tasks/:id/rate": "DEBT high, wants task:write — 200. An app grant holding any single scope — memory:read is enough — can attach a 1-5 star rating with free-form comment and metadata to any DONE task of its owner, stamped raterType 'agent' or 'source-grounded-agent', and that rating is then baked into the agent's PUBLIC statistics keys. This is t… [triaged 2026-08-16]",
    "src/routes/agent-tasks/completion.ts:PATCH:/v1/agents/:name/tasks/:id/todos/:todoId": "GATED IN THE HANDLER OR THE SERVICE — canAccessTask() (src/routes/agent-tasks.ts:104-112) authorizes against the loaded task record, so the client-supplied task id is checked rather than trusted, and app grants are excluded from every write route by design. [triaged 2026-08-16]",
    "src/routes/agent-tasks/completion.ts:GET:/v1/agents/:name/tasks/:id/events": "GATED IN THE HANDLER OR THE SERVICE — Authorization is on the task record, not the path: canReadTask() (src/routes/agent-tasks.ts:119-125) admits the task's owner, the task's agent, or a same-owner app grant that explicitly holds task:read, and refuses everything else. [triaged 2026-08-16]",
    "src/routes/agent-tasks/create-read.ts:POST:/v1/agents/:name/tasks": "GATED IN THE HANDLER OR THE SERVICE — The handler names the scope itself: an app caller must hold task:write and target its own owner's agent (create-read.ts:86-94), an agent caller must share the owner (77-83), anything else is refused at 73. [triaged 2026-08-16]",
    "src/routes/agent-tasks/create-read.ts:GET:/v1/agents/:name/tasks": "GATED IN THE HANDLER OR THE SERVICE — The handler is the gate: an app principal without task:read is refused (create-read.ts:161), an agent may list only its own tasks (167-171), and the owner/app path is fenced by listAgentTasksByOwner(resolveIdentity(...)). [triaged 2026-08-16]",
    "src/routes/agent-tasks/create-read.ts:GET:/v1/agents/:name/tasks/:id": "GATED IN THE HANDLER OR THE SERVICE — canReadTask (agent-tasks.ts:119-125) decides: owner or the task's own agent, or a same-owner app grant holding task:read; every other caller gets 403 at create-read.ts:233. [triaged 2026-08-16]",
    "src/routes/agent-tasks/lifecycle.ts:PATCH:/v1/agents/:name/tasks/:id": "GATED IN THE HANDLER OR THE SERVICE — canAccessTask (agent-tasks.ts:104-112) is the gate at lifecycle.ts:51: the task's own owner session or the task's own agent, and an app grant is excluded by construction. [triaged 2026-08-16]",
    "src/routes/agent-tasks/lifecycle.ts:POST:/v1/agents/:name/tasks/:id/start": "GATED IN THE HANDLER OR THE SERVICE — The handler names the scope: an app caller must hold task:write (lifecycle.ts:305) and own the task (319); everything else is owner-session only (301), the same rule /queue states as requireScope('task:write'). [triaged 2026-08-16]",
    "src/routes/agent-tasks/lifecycle.ts:POST:/v1/agents/:name/tasks/:id/propose-todos": "GATED IN THE HANDLER OR THE SERVICE — canAccessTask (agent-tasks.ts:104-112) at lifecycle.ts:417 admits only the task's owner session or the task's own agent, which is the same rule the MCP twin is exempted on (mcp/catalog/scopes.ts:97). [triaged 2026-08-16]",
    "src/routes/agent-tasks/lifecycle.ts:POST:/v1/agents/:name/tasks/:id/request-changes": "GATED IN THE HANDLER OR THE SERVICE — Owner-session only (roles owner AND NOT agent, lifecycle.ts:453) plus canAccessTask on the task itself (467); the agent whose plan is being revised is deliberately excluded. [triaged 2026-08-16]",
    "src/routes/agent-tasks/lifecycle.ts:POST:/v1/agents/:name/tasks/:id/pause": "GATED IN THE HANDLER OR THE SERVICE — Owner-session only, tested as roles owner AND NOT agent (lifecycle.ts:559), plus canAccessTask for the task itself (573); an app grant and the agent being paused are both refused. [triaged 2026-08-16]",
    "src/routes/agent-telemetry.ts:POST:/v1/agents/:name/telemetry": "GATED IN THE HANDLER OR THE SERVICE — canAccessAgent (agent-telemetry.ts:49-57) admits the owner session or the reporting agent itself, and every write is keyed to the GAII built from the caller's own owner, which is the reason already recorded for the MCP twin at mcp/catalog/scopes.ts:81. [triaged 2026-08-16]",
    "src/routes/agent-telemetry.ts:GET:/v1/agents/:name/telemetry": "GATED IN THE HANDLER OR THE SERVICE — canAccessAgent (agent-telemetry.ts:49-57) admits the owner session or the agent itself and returns false for every other principal class, and the GAII is built under the caller's own owner so cross-owner is unreachable. [triaged 2026-08-16]",
    "src/routes/agent-webhook.ts:GET:/v1/agents/:name/integration/overview": "GATED IN THE HANDLER OR THE SERVICE — The composite carries the same canAccessAgent gate as the three reads it folds (agent-webhook.ts:67, definition at 47-55): the owner session or the agent itself, on a GAII built under the caller's own owner. [triaged 2026-08-16]",
    "src/routes/agent-webhook.ts:PUT:/v1/agents/:name/webhook": "GATED IN THE HANDLER OR THE SERVICE — canAccessAgent (agent-webhook.ts:47-55) admits the owner session or the agent itself; the response carries the webhook secret, so no third principal class may reach this door, and the URL is SSRF-checked by validateOutboundUrl (110). [triaged 2026-08-16]",
    "src/routes/agent-webhook.ts:GET:/v1/agents/:name/webhook": "GATED IN THE HANDLER OR THE SERVICE — canAccessAgent (agent-webhook.ts:47-55) admits the owner session or the agent itself, and the response omits webhookSecret, so the read carries no credential. [triaged 2026-08-16]",
    "src/routes/agent-webhook.ts:DELETE:/v1/agents/:name/webhook": "GATED IN THE HANDLER OR THE SERVICE — canAccessAgent (agent-webhook.ts:47-55) admits the owner session or the agent itself, and the target GAII is built under the caller's own owner, so no other principal and no other owner can clear the webhook. [triaged 2026-08-16]",
    "src/routes/agent-webhook.ts:POST:/v1/agents/:name/webhook/test": "GATED IN THE HANDLER OR THE SERVICE — canAccessAgent (agent-webhook.ts:47-55) admits the owner session or the agent itself, and the request goes to the stored URL through safeFetch (242), so the caller cannot choose the destination. [triaged 2026-08-16]",
    "src/routes/agent-webhook.ts:GET:/v1/agents/:name/webhook/log": "GATED IN THE HANDLER OR THE SERVICE — canAccessAgent (agent-webhook.ts:47-55) admits the owner session or the agent itself, on a GAII built under the caller's own owner; the log holds full dispatched payloads, so no other principal reaches it. [triaged 2026-08-16]",
    "src/routes/agents/device-auth.ts:POST:/v1/agents/device-authorize": "OPEN BY DESIGN — RFC 8628 step one, which by definition has no session; an unauthenticated call only creates a pending record, bounded by ten per owner (device-auth.ts:337) and an expiry, and the same-owner auto-approve branch refuses app and ecosystem principals and cross-owner callers (282-292). [triaged 2026-08-16]",
    "src/routes/agents/device-auth.ts:POST:/v1/agents/device-token": "OPEN BY DESIGN — RFC 8628 poll: the unguessable device_code is the credential and no session can exist yet, bounded by the expiry check (device-auth.ts:471), the slow_down backoff (480-491) and the 120 s retrieval window (524-531). [triaged 2026-08-16]",
    "src/routes/agents/device-auth.ts:POST:/v1/agents/verify": "GATED IN THE HANDLER OR THE SERVICE — The approving owner's JWT is carried in the body and verified in the handler: verifyJWT plus a roles check (device-auth.ts:620-624) and an owner match against the pending request (644-647), so only the account holder can approve their own device request. [triaged 2026-08-16]",
    "src/routes/agents/offers.ts:GET:/v1/agents/:name/offers": "DEBT medium, wants memory:read — An app grant reads EVERYTHING its own owner's agents publish, private offers and their prices included, holding whatever single scope the consent screen showed: `owner` at line 69 is `req.auth!.owner`, which on an app grant is the human's account name, so isOwnerOfAgent is true at 76 and no scope i… [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/agents/offers.ts:POST:/v1/agents/:name/offers/:offerId/invoke": "DEBT high, wants work:request — An app-grant token holding one unrelated scope can invoke any callable offer and spend the owner's morsels. resolveIdentity on roles ['app'] returns the grant's sub, which IS the owner GHII (app-grants.ts:522, :456), so debitBalance draws straight on the human's balance and the money layer cannot t… [triaged 2026-08-16]",
    "src/routes/agents/profile-metadata.ts:GET:/v1/agents": "DEBT medium, wants task:read — The owner's entire fleet inventory, on any single scope. `req.auth!.owner` is the human's account name on an app grant, so the query is answered in full; and `?include=stats` hands the app active task ids and titles that GET /v1/agents/:name/tasks would have refused it for want of `task:read`, whic… [triaged 2026-08-16]",
    "src/routes/agents/profile-metadata.ts:PATCH:/v1/agents/:name/tags": "DEBT medium, wants agent:write — It can rewrite the tag list of any of its owner's agents on one unrelated scope, for the same reason as /mode: req.auth!.owner is the human's name, so agent-profile-write.ts:110 passes. `agent:write` is not in APP_GRANTABLE_SCOPES, so naming the scope here would close the door to apps outright. [triaged 2026-08-16]",
    "src/routes/agents/profile-metadata.ts:PATCH:/v1/agents/:name/mode": "DEBT high, wants agent:write — It can flip any of its owner's agents into `task-runner`, holding one unrelated scope. roles ['app'] passes the only check because req.auth!.owner is the human's name (agent-profile-write.ts:110). Gating this the way /console-url is gated would shut apps out entirely and correctly: `agent:write` is… [triaged 2026-08-16]",
    "src/routes/agents/registration.ts:POST:/v1/agents/connect": "GATED IN THE HANDLER OR THE SERVICE — The connectivity key is the credential: consumeOtk validates and burns it single-use with an expiry (registration.ts:44, storage auth.ts:57-87), only an OTK whose action is register_agent is accepted, and the owner comes from the key rather than the request. [triaged 2026-08-16]",
    "src/routes/ai-transparency.ts:GET:/v1/ai-transparency/mine": "DEBT low, wants wallet:read (the word this repo already uses for reading the owner's own activity ledger — src/routes/usage-reports.ts:69, USAGE_READ_SCOPE = 'wallet:read' — and this route's own next-action hint points at /v1/ledger/usage; there is no provenance:read in the vocabulary) — The owner's full AI-publishing exposure: the transparency report, the 25 most recent provenance rows (principal, content hash, model, pipeline, generated_at) and the unlabelled-public list. An app-grant token's `sub` IS the owner GHII (src/utils/gaii.ts:145-148, and the app-grant note at 168-181)… [triaged 2026-08-16]",
    "src/routes/ai-transparency.ts:GET:/v1/ai-transparency/logging-policy": "OPEN BY DESIGN — Node-level policy derived from config alone: the handler takes no request input and reads no storage, and the same facts are already served unauthenticated at /v1/ai-transparency. [triaged 2026-08-16]",
    "src/routes/ai.ts:POST:/v1/ai/complete": "GATED IN THE HANDLER OR THE SERVICE — ai:use is enforced in the handler by assertAiUseAllowed (src/auth/ai-gate.ts:26-33), the same test the chat proxy and POST /v1/ai/image apply; the key and the budget are resolved from resolveIdentity, never from the body. [triaged 2026-08-16]",
    "src/routes/ai.ts:POST:/v1/ai/transcribe": "GATED IN THE HANDLER OR THE SERVICE — ai:use is enforced in the handler by assertAiUseAllowed (src/auth/ai-gate.ts:26-33), and the audio key is resolved against the caller's own namespace at ai.ts:198. [triaged 2026-08-16]",
    "src/routes/ai.ts:GET:/v1/ai/available": "GATED IN THE HANDLER OR THE SERVICE — ai:use is enforced in the handler by assertAiUseAllowed (src/auth/ai-gate.ts:26-33), the one rule every door that spends the owner's AI budget shares. [triaged 2026-08-16]",
    "src/routes/ai.ts:GET:/v1/admin/ai-usage": "GATED IN THE HANDLER OR THE SERVICE — Operator-only, decided in the handler at ai.ts:367 (`req.auth.roles.includes('operator')` → 403) — the same test requireRole('operator') makes, written one line lower than the scanner can see. [triaged 2026-08-16]",
    "src/routes/app-grants.ts:POST:/v1/app-grants/token": "OPEN BY DESIGN — An OAuth-style token exchange runs before any session exists, by definition. It is bound instead by single-use 60s codes, PKCE, redirect_uri equality, and a refresh-hash lookup that refuses a revoked grant. [triaged 2026-08-16]",
    "src/routes/app-members.ts:GET:/v1/apps/:owner/:filename/members": "GATED IN THE HANDLER OR THE SERVICE — The roster is owner-only, plus an app-declared members-can-see-members branch that still refuses non-members and redacts notes, approvers and carried offerings (app-members.ts:74-91). [triaged 2026-08-16]",
    "src/routes/app-members.ts:GET:/v1/apps/:owner/:filename/members/me": "OPEN BY DESIGN — Open to any authenticated caller by design: it answers only about the caller (app-members.ts:109, :122-123) and the visit it records is the caller's own, throttled hourly into a system namespace. [triaged 2026-08-16]",
    "src/routes/app-members.ts:GET:/v1/apps/:owner/:filename/members/plan": "GATED IN THE HANDLER OR THE SERVICE — Owner-only, decided at app-members.ts:137 against the resolveIdentity-resolved caller (:59-60) — the plan says what the owner gives away, so it is not another member's business. [triaged 2026-08-16]",
    "src/routes/app-members.ts:PUT:/v1/apps/:owner/:filename/members/plan": "DEBT high, wants commerce:sell — It rewrites the carry plan of its own owner's apps on any scope: which roles are carried on which offerings, seat counts, term lengths and roster visibility. Existing members are deliberately not re-synced (:213-215), so the blast is limited to approvals made after it. Another owner's plan is refus… [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/app-members.ts:DELETE:/v1/apps/:owner/:filename/members/seen/:account": "GATED IN THE HANDLER OR THE SERVICE — Owner-only, decided at app-members.ts:225 against the resolveIdentity-resolved caller (:59-60); it dismisses one guest row that reappears on the next visit. [triaged 2026-08-16]",
    "src/routes/app-members.ts:POST:/v1/apps/:owner/:filename/members/sweep": "GATED IN THE HANDLER OR THE SERVICE — Owner-only, decided at app-members.ts:239 against the resolveIdentity-resolved caller (:59-60); it only closes memberships whose term already expired, which the hourly timer does anyway. [triaged 2026-08-16]",
    "src/routes/app-members.ts:POST:/v1/apps/:owner/:filename/members": "DEBT high, wants exchange:grant — It approves members into its own owner's apps and, through syncGrantsForMember (:322-328), hands them free access to what that owner sells — on whatever single scope it holds. The vocabulary already has the word for that favour (`exchange:grant`, app-grants.ts:136, enforced with requireScope twice… [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/app-members.ts:DELETE:/v1/apps/:owner/:filename/members/:account": "DEBT medium, wants exchange:grant — Full removal powers over any app of its own owner, holding whatever single scope the consent screen showed. resolveIdentity (utils/gaii.ts:145-149) returns auth.sub for a role-'app' token, and an app grant's sub IS the owner GHII (app-grants.ts:522), so accountOf reduces it to 'alice' and isOwner i… [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/app-members.ts:POST:/v1/apps/:owner/:filename/members/requests": "DEBT medium, wants social:write — It can make its own owner ask to join an app and leave a 400-character note. It cannot ask on anybody else's behalf — the account is resolved, never taken from the body — and it gains no access; a separate owner-only approval does that. [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/app-members.ts:DELETE:/v1/apps/:owner/:filename/members/requests/:account": "GATED IN THE HANDLER OR THE SERVICE — Declining an ask is owner-only, decided at app-members.ts:432 against the caller resolved through resolveIdentity at :59-60. [triaged 2026-08-16]",
    "src/routes/app-store.ts:POST:/v1/app-store/purchase": "DEBT high, wants contract:spend — It spends the human's morsels. Any app grant reaches this holding whatever single scope the consent screen showed (memory:read is enough), and debitBalance resolves any principal to the owner GHII, so the human pays. The per-app ceiling is not consulted here either — spendCapMorsels/spentMorsels (a… [triaged 2026-08-16]",
    "src/routes/app-store.ts:GET:/v1/app-store/purchases": "DEBT low, wants wallet:read — An app grant on any scope reads its owner's app-purchase history: what they bought, from whom, at what price and when. It is a read of the owner's own money record with no word asked for it, and the same rows are wallet transactions (type 'app_purchase', :108-116) that wallet:read fences elsewhere. [triaged 2026-08-16]",
    "src/routes/app-store.ts:GET:/v1/app-store/purchases/:txId": "GATED IN THE HANDLER OR THE SERVICE — A receipt is readable by its buyer or its seller only, compared in the handler at app-store.ts:224 before the paid app bytes are returned. [triaged 2026-08-16]",
    "src/routes/app-store.ts:GET:/v1/app-store/sales": "DEBT low, wants wallet:read — An app grant on any scope reads its owner's sales ledger: every buyer's owner name, the price and the fee taken. Revenue plus a customer list of the owner's own account, with no word asked for it. [triaged 2026-08-16]",
    "src/routes/app-store.ts:GET:/v1/app-store/license-check": "DEBT low, wants wallet:read — Any app grant reads whether its owner holds a licence for a named app, holding whatever single scope it was given. Note the sub-form split: an owner browser session carries sub='alice' (services/owner-session.ts:134) and an app grant carries sub='alice@node' (app-grants.ts:522), and this route comp… [triaged 2026-08-16]",
    "src/routes/appdev-overview.ts:GET:/v1/appdev/overview": "DEBT low, wants memory:read — An app grant on any scope gets a digest of its owner's app-development memory — learned pitfalls, template proposals and skills under the owner's namespace — alongside the public catalogue. Self-scoped, so no other owner's data, but it is memory the owner never granted a read of. [triaged 2026-08-16]",
    "src/routes/appdev-overview.ts:GET:/v1/appdev/templates": "DEBT low, wants memory:read — An app grant on any scope lists its owner's template proposals with full manifests — memory records under the owner's GHII. No cross-owner reach, but the write sibling asks for memory:write while this read asks for nothing. [triaged 2026-08-16]",
    "src/routes/appdev-overview.ts:GET:/v1/appdev/templates/:id": "DEBT low, wants memory:read — An app grant on any scope reads one of its owner's template proposals in full, plus the source app's live state (forkable, version). Keyed to the owner's own GHII, so guessing another owner's proposal id returns 404. [triaged 2026-08-16]",
    "src/routes/appdev-pitfalls.ts:GET:/v1/appdev/pitfalls/learned": "DEBT low, wants memory:read — An app grant on any scope reads its owner's private learned pitfalls in full body, including ones the owner never shared. Other owners' entries appear only when explicitly shared, so this is a read of the owner's own memory records with no word asked for it while the write siblings ask for memory:w… [triaged 2026-08-16]",
    "src/routes/appeals.ts:POST:/v1/flags/:flagId/appeal": "GATED IN THE HANDLER OR THE SERVICE — src/routes/appeals.ts:80 — router.post('/v1/flags/:flagId/appeal', requireAuth(), ...) with no gate middleware; the handler loads the flag (96), resolves the flagged content's owner (104), and decides at 108-123: `let isOwner = contentOwner === caller;` (caller = req.auth!.sub, line 103), widened a… [triaged 2026-08-16]",
    "src/routes/appeals.ts:GET:/v1/appeals": "GATED IN THE HANDLER OR THE SERVICE — Operator or organism admin, decided in the handler: appeals.ts:179-183 refuses anyone who is neither, after resolving the caller's admin organisms at 162-176. [triaged 2026-08-16]",
    "src/routes/appeals.ts:POST:/v1/appeals/:id/review": "GATED IN THE HANDLER OR THE SERVICE — Operator or organism admin, decided in the handler at appeals.ts:258-262 after resolving the target's organism and the caller's admin membership at 240-256. [triaged 2026-08-16]",
    "src/routes/apps/agents-deploy.ts:POST:/v1/apps/:owner/:filename/agents/:agentName/deploy": "DEBT medium, wants task:write — Refused unless it holds task:write (or task:* / *) — the scope test at :62 runs before anything is created. Same two caveats as status: the wildcard test at :39-43 is a copy of the rule rather than scopeIsCovered, and an agent principal passes with no scope test. [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/apps/agents-deploy.ts:POST:/v1/apps/:owner/:filename/agents/:agentName/undeploy": "DEBT medium, wants task:write — Refused unless it holds task:write (or task:* / *). Within that word it can only undeploy on its own owner's fleet — the pointer task is created on a runner resolved under ctx.owner (:121, :131-133). [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/apps/agents-deploy.ts:GET:/v1/apps/:owner/:filename/agents/:agentName/status": "GATED IN THE HANDLER OR THE SERVICE — resolveDeployContext performs the decision the scanner cannot see: an app grant must carry task:read (agents-deploy.ts:62) and any client-supplied owner that is not the requester's is 403 (:73-79), so status is read on the caller's own fleet only. [triaged 2026-08-16]",
    "src/routes/apps/drafts.ts:PUT:/v1/apps/:owner/:filename/draft": "DEBT medium, wants app:write — It replaces its owner's entire draft for any app on any scope. The file's own v2.4.0 note says the four incremental editors added on 2026-08-16 'each asks for app:write rather than requireAuth alone' (drafts.ts:135, :179, :213, :243) — this door performs the same act on the same slot and asks for n… [triaged 2026-08-16]",
    "src/routes/apps/drafts.ts:GET:/v1/apps/:owner/:filename/draft": "DEBT low, wants app:write — It reads its owner's unpublished draft in full on any scope. The identical read on a line range, GET .../draft/lines at drafts.ts:213, already carries requireScope('app:write') — the same slot, two doors, one gate. [triaged 2026-08-16]",
    "src/routes/apps/drafts.ts:POST:/v1/apps/:owner/:filename/draft/preview-token": "DEBT medium, wants app:write — It mints a 10-minute unauthenticated URL onto its owner's unpublished draft, on any scope, and whoever holds that link reads the work. The token names one app and one owner, and it expires, which is what bounds the damage. [triaged 2026-08-16]",
    "src/routes/apps/drafts.ts:POST:/v1/apps/:owner/:filename/frame-token": "DEBT high, wants app:write — On any scope it mints a 12-hour grant letting one named origin embed its owner's LIVE published app. The header the grant is written from is browser-controlled only for browsers: a server-side caller (an agent, a grant used from a backend) sets Origin freely, so the origin in the grant is the calle… [triaged 2026-08-16]",
    "src/routes/apps/drafts.ts:DELETE:/v1/apps/:owner/:filename/draft": "DEBT medium, wants app:write — It destroys its owner's unpublished working copy for any app, holding whatever single scope the consent screen showed, because req.auth.owner carries the human's account name on an app grant. The three sibling write doors in this same file (draft/write :135, draft/replace :179, draft/seed :243) alr… [triaged 2026-08-16]",
    "src/routes/apps/drafts.ts:POST:/v1/apps/:owner/:filename/publish-draft": "DEBT high, wants app:write — It publishes bytes live under its owner's name, on any scope. This is the deferred step three named in app-grants.ts:155-177: 'app:write' entered the grantable vocabulary on 2026-08-11 (step one) and the publish doors were deliberately left ungated so live apps publishing through a grant (ORIGAMI… [triaged 2026-08-16]",
    "src/routes/apps/fork-manage.ts:POST:/v1/apps/:owner/:filename/fork": "DEBT medium, wants app:write — A new published app under the owner's name. The app grant satisfies `sameOwner` for the owner's own apps and passes :78 for any app flagged forkable; because `req.auth!.sub` is the owner's GHII it also inherits the owner's licenses at :85, so the paywall does not separate it from the human either… [triaged 2026-08-16]",
    "src/routes/apps/fork-manage.ts:PATCH:/v1/apps/:filename": "DEBT high, wants app:write — Full control of every app in the owner's catalogue. One unrelated scope is enough to strip an app's access code (:258, whose own note reads 'The app is now publicly downloadable'), unpark a hidden app into the public catalogue (:269), turn on forkable so strangers may copy it (:280), rewrite name a… [triaged 2026-08-16]",
    "src/routes/apps/fork-manage.ts:DELETE:/v1/apps/:filename": "DEBT high, wants app:manage — Everything the owner can do. An app grant is `roles:['app']` with `sub` = the owner's GHII (app-grants.ts:517-522), so canonicalOwner resolves it to the human and deleteOwnedApp removes any of the owner's apps, all versions, plus the screenshot. requireScope is the only middleware that would stop i… [triaged 2026-08-16]",
    "src/routes/apps/publish.ts:POST:/v1/apps": "DEBT high, wants app:write — Publish, and update in place, any app in the owner's catalogue, each one at a public URL anyone with the link can open. A new version overwrites the bytes served at an existing app's live address. The consent screen line for this (app-grants.ts:178) exists and is deliberately not enforced here yet. [triaged 2026-08-16]",
    "src/routes/apps/read.ts:POST:/v1/apps/:owner/:filename/screenshot": "DEBT medium, wants app:write — Replaces the public tile image of any of the owner's apps with arbitrary caller-supplied bytes served publicly under the owner's app URL. The owner-name comparison at :291 admits every principal acting in the owner's name, an app grant included. [triaged 2026-08-16]",
    "src/routes/apps/read.ts:DELETE:/v1/apps/:owner/:filename/screenshot": "DEBT low, wants app:write — Clears the public screenshot of any app the owner owns. `canonicalOwner` reads `req.auth!.owner`, which on an app grant is the human's account name, so :357 passes by construction. Another owner's app is genuinely refused by that same line. [triaged 2026-08-16]",
    "src/routes/apps-cost.ts:GET:/v1/apps/cost": "DEBT low, wants exchange:read — Everything. An app grant carries roles:['app'] with sub = the owner's GHII (app-grants.ts:522, and resolveIdentity returns sub unchanged for a non-owner session, gaii.ts:145-148), so :83 matches and the app reads the owner's whole EXCHANGE cost position for any app_id it names: every entitlement, p… [triaged 2026-08-16]",
    "src/routes/attestations.ts:POST:/v1/attestations": "DEBT medium, wants memory:write-reserved — It can create an OPEN attestation that names its own owner as a party (sub = owner GHII) and nothing else — a record naming only third parties is 403 CREATOR_NOT_PARTY. The record is unsigned and inert until a private key signs it, so the residual is record noise under the reserved sys:attest names… [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/attestations.ts:POST:/v1/attestations/:id/sign": "GATED IN THE HANDLER OR THE SERVICE — Two decisions the scanner cannot see: only a listed party may sign (attestation.ts:135-137), and the signature must verify against that party's registered Ed25519 key (145-149) — a cryptographic gate, stronger than any scope word. [triaged 2026-08-16]",
    "src/routes/auth.ts:POST:/v1/auth/anonymous": "OPEN BY DESIGN — Public by design and only on a node that opted in: the handler refuses with 403 unless anonymous mode is enabled, and the token it issues names the node's shared anonymous identity rather than any account. [triaged 2026-08-16]",
    "src/routes/auth.ts:POST:/v1/auth/token": "OPEN BY DESIGN — The credential-issuing door, so by definition no session exists yet: both branches prove possession of an Ed25519 private key against the stored public key and refuse a timestamp older than five minutes. [triaged 2026-08-16]",
    "src/routes/auth.ts:POST:/v1/auth/refresh": "GATED IN THE HANDLER OR THE SERVICE — Authorized by principal class inside the handler (auth.ts:483-520): an agent refreshes as ['agent'] with its stored scopes, an ecosystem app as ['ecosystem'] with its own, an app grant is refused 403 and sent to POST /v1/app-grants/token, and the cookie paths re-validate the presented refresh token… [triaged 2026-08-16]",
    "src/routes/auth.ts:POST:/v1/auth/revoke": "GATED IN THE HANDLER OR THE SERVICE — There is nothing to authorize: every target is derived from the credential the caller presented, its own bearer and its own httpOnly path-scoped refresh cookie, and no session id is accepted from the request. [triaged 2026-08-16]",
    "src/routes/auth.ts:GET:/v1/auth/sessions": "DEBT medium, wants account:security — The human's device list, session ids included. An app approved for memory:read alone learns where and when the person signs in, and the session_id values it reads are precisely the ids DELETE /v1/auth/sessions/:id accepts. [triaged 2026-08-16]",
    "src/routes/auth.ts:DELETE:/v1/auth/sessions/:id": "DEBT medium, wants account:security — Revoke any single browser session belonging to the human. The listActiveSessions(owner) lookup refuses a different PERSON's session id but admits everything acting in this person's name, which is exactly the owner-is-not-a-principal trap; the session ids to name are handed out by GET /v1/auth/sessi… [triaged 2026-08-16]",
    "src/routes/auth.ts:DELETE:/v1/auth/sessions": "DEBT high, wants account:security — Sign the person out of every device they own. Since 2026-08-13 session revocation is actually enforced (auth/middleware.ts optionalAuth), so this is a live account control, and an app approved for one scope reaches it. It is the same family of door as the password and recovery address, which requir… [triaged 2026-08-16]",
    "src/routes/boards.ts:PATCH:/v1/boards/:boardId/members": "GATED IN THE HANDLER OR THE SERVICE — An owner SESSION is required in the handler (boards.ts:161) and it must own the board (170); agents, ecosystem apps and app grants are all refused there, which is stricter than any scope word. [triaged 2026-08-16]",
    "src/routes/capabilities.ts:POST:/v1/capabilities/:id/invoke": "DEBT high, wants work:request — An app grant holding any single scope (catalogue:read is enough) invokes every public capability on the node, and every PRIVATE capability of its own owner as well: line 294 compares resolve(req) against cap.ownerGhii, and an app token's `sub` IS the owner GHII (routes/app-grants.ts:522, utils/gaii… [triaged 2026-08-16]",
    "src/routes/capabilities.ts:POST:/v1/capabilities/:id/telemetry": "DEBT medium, wants work:request — Any app grant, whatever single scope it holds, increments success/error counts and totalMs on any capability that exists, including another owner's public one. Its error text is refused unless the app's owner owns the capability (:342) and its duration is clamped to one hour (:338), so what it gets… [triaged 2026-08-16]",
    "src/routes/catalogue.ts:GET:/v1/catalogue/directory": "DEBT high, wants catalogue:read (already in the vocabulary — SCOPE_DOMAINS `{ key: 'catalogue', permissions: ['read'] }` in public/views/profile/agents/scope-model.js, and one of the three anonymous default scopes at src/auth/middleware.ts:254 — but enforced on no route today) — The whole people-and-organism directory, 50 entries per page with facets: ghii, displayName, bio, avatar, interests, city/area/country and lat/lon for everyone who opted in, plus organism entries with memberCount and joinPolicy. Bulk data about OTHER owners, reached with whatever single scope the c… [triaged 2026-08-16]",
    "src/routes/chat-instances.ts:POST:/v1/chat-instances": "OPEN BY DESIGN — Self-targeted by construction: the row id is built from req.auth.owner and never from the body (services/chat-instance-write.ts:39-44), and the MCP twin aimeat_instance_create is already a recorded scope exemption for the same reason (src/mcp/catalog/scopes.ts:83). [triaged 2026-08-16]",
    "src/routes/chat-instances.ts:GET:/v1/chat-instances": "OPEN BY DESIGN — Self-scoped bookkeeping: the list is keyed by req.auth.owner taken from the token, holds no content and cannot be pointed at another account; the economy fields live on the per-id route, which carries its own ownership check. [triaged 2026-08-16]",
    "src/routes/chat-instances.ts:GET:/v1/chat-instances/:id": "DEBT high, wants wallet:read — It passes for its own owner's rows, and the payload includes the GHII's trust_score and morsel_balance (lines 113-117) — the money figure that /v1/wallet fences behind requireScope('wallet:read') (src/routes/wallet.ts:51). Another owner's instance is 404. So: cross-owner closed, but an app grant re… [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/chat-instances.ts:PUT:/v1/chat-instances/:id": "GATED IN THE HANDLER OR THE SERVICE — Ownership is compared in the handler (chat-instances.ts:132) before the only write, which is a lastSeen heartbeat; a foreign row answers 404. [triaged 2026-08-16]",
    "src/routes/chat-instances.ts:DELETE:/v1/chat-instances/:id": "GATED IN THE HANDLER OR THE SERVICE — Ownership is compared in the handler (chat-instances.ts:154, record.ghii owner vs req.auth.owner, operator excepted) and a foreign or missing row answers an identical 404. [triaged 2026-08-16]",
    "src/routes/commerce-acp.ts:POST:/acp/v1/checkout_sessions": "DEBT medium, wants commerce:buy — It can open priced checkout sessions in the owner's name against any seller on the node, with no scope asked, and write one memory record per session into the owner's namespace (which has a 1000-key ceiling). It cannot complete one — the spend gate bites at completion, not here. mcp/catalog/scopes… [triaged 2026-08-16]",
    "src/routes/commerce-acp.ts:GET:/acp/v1/checkout_sessions/:id": "DEBT low, wants commerce:buy — Full read of any of the owner's checkout sessions by id — line items, unit prices, totals, the receipt and its tracking code — on whatever single scope was approved. The same capability on the MCP surface (`aimeat_checkout_list`) requires commerce:buy, mcp/catalog/scopes.ts:400. [triaged 2026-08-16]",
    "src/routes/commerce-acp.ts:POST:/acp/v1/checkout_sessions/:id/complete": "DEBT high, wants commerce:buy — 403 SCOPE_REQUIRED unless the grant carries `contract:spend`, and then only up to the per-app ceiling the owner set; a grant created before 2026-07-28 is grandfathered through (metered-access.ts:198-202). This is the one money-moving door in the slice where the app-grant question is genuinely answe… [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/commerce-beneficiaries.ts:GET:/v1/commerce/beneficiary-splits": "DEBT medium, wants exchange:beneficiary — It reads the owner's entire revenue-sharing arrangement — every metered coordinate, pool_percent, role chain with its percentages, and every beneficiary GHII — on any single approved scope. The MCP tool for this exact read, `aimeat_commerce_beneficiary_splits`, is gated on exchange:beneficiary (mcp… [triaged 2026-08-16]",
    "src/routes/commerce-beneficiaries.ts:GET:/v1/commerce/beneficiary/approvals": "GATED IN THE HANDLER OR THE SERVICE — The cross-account read is refused in the handler: commerce-beneficiaries.ts:368 requires the operator role before `?ghii=` may name anyone but the caller's own resolved owner, so without it the route reports only the caller's own approval state. [triaged 2026-08-16]",
    "src/routes/commerce-ucp.ts:POST:/ucp/v1/checkout-sessions": "DEBT medium, wants commerce:buy — Opens priced checkout sessions in the owner's name, in any currency the schema accepts (:59), against any seller — on any single approved scope. Completion is separately gated; creation is not. [triaged 2026-08-16]",
    "src/routes/commerce-ucp.ts:GET:/ucp/v1/checkout-sessions/:id": "DEBT low, wants commerce:buy — Reads any of the owner's checkout sessions in the UCP shape — items, unit prices, grand total, receipt and tracking code — on any single approved scope. [triaged 2026-08-16]",
    "src/routes/commerce-ucp.ts:PATCH:/ucp/v1/checkout-sessions/:id": "DEBT medium, wants commerce:buy — It can cancel any of the owner's open sessions, or replace the cart entirely — including swapping in a different seller and a different total, since resolveItems recomputes sellerOwner from the new items (session-service.ts:87). What the owner later pays for is therefore writable by a principal tha… [triaged 2026-08-16]",
    "src/routes/commerce-ucp.ts:POST:/ucp/v1/checkout-sessions/:id/complete": "DEBT high, wants commerce:buy — 403 SCOPE_REQUIRED without `contract:spend`, and capped by the owner's per-app spend limit once it has it; pre-2026-07-28 grants are grandfathered (metered-access.ts:198-202). [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/commerce-webhooks.ts:POST:/v1/commerce/webhooks/stripe/:owner": "OPEN BY DESIGN — Public by design: Stripe is the caller and no session exists. Authorization is the seller's own endpoint signing secret, verified as a constant-time HMAC over the raw body with a 5-minute tolerance (commerce-webhooks.ts:99) behind an IP rate limit; no bearer token is ever read. [triaged 2026-08-16]",
    "src/routes/commerce.ts:POST:/v1/commerce/checkout-sessions": "DEBT medium, wants commerce:buy — Opens priced checkout sessions in the owner's name against any seller and in any currency, on any single approved scope. mcp/catalog/scopes.ts:355-358 states the split as a known one: the REST routes are requireAuth-only and `aimeat_checkout_open` was gated commerce:buy on purpose. [triaged 2026-08-16]",
    "src/routes/commerce.ts:GET:/v1/commerce/checkout-sessions": "DEBT low, wants commerce:buy — The owner's whole purchase history — up to 200 sessions with items, totals, receipts and tracking codes — on any single approved scope. `aimeat_checkout_list`, the same read over MCP, requires commerce:buy (mcp/catalog/scopes.ts:400). [triaged 2026-08-16]",
    "src/routes/commerce.ts:GET:/v1/commerce/orders": "DEBT medium, wants wallet:read — The owner's sales book: every completed order with the BUYER's GHII, what they bought, what they paid, the seller's net, the platform fee and the tracking code. That is other people's identities and the owner's revenue, readable on any single approved scope. [triaged 2026-08-16]",
    "src/routes/commerce.ts:GET:/v1/commerce/checkout-sessions/:id": "DEBT low, wants commerce:buy — Any one of the owner's sessions in full, including the completed ones with their payment receipt and tracking code, on any single approved scope. [triaged 2026-08-16]",
    "src/routes/commerce.ts:PATCH:/v1/commerce/checkout-sessions/:id": "DEBT medium, wants commerce:buy — It can cancel the owner's open sessions or replace their contents — a different seller and a different total included, since resolveItems recomputes the seller from the new items (session-service.ts:87). The write that decides what a later completion charges for is unscoped. [triaged 2026-08-16]",
    "src/routes/commerce.ts:GET:/v1/commerce/holds": "DEBT low, wants wallet:read — The owner's whole hold book on either side (`?side=seller` for inbound): amounts authorised on the owner's payment instrument but not yet moved, counterparties, purposes, references and rail tracking codes — on any single approved scope. [triaged 2026-08-16]",
    "src/routes/commerce.ts:GET:/v1/commerce/holds/:id": "DEBT low, wants wallet:read — Any hold the owner is buyer or seller of, in full, including the money authorised on their card and the rail tracking code — on any single approved scope, while releasing that same hold needs commerce:sell (:424). [triaged 2026-08-16]",
    "src/routes/commerce.ts:POST:/v1/commerce/checkout-sessions/:id/complete": "DEBT high, wants commerce:buy — 403 SCOPE_REQUIRED unless the grant holds `contract:spend`, then bounded by the per-app spendCapMorsels ceiling; grants predating 2026-07-28 pass without the word (metered-access.ts:198-202). This is the H-3 fix and it holds on this door. [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/connections.ts:GET:/v1/connections/providers": "OPEN BY DESIGN — Discovery only: a config-derived provider list with no credential and no per-principal data (services/connections/providers.ts:553-571), while every route in this file that touches a real connection carries a scope. [triaged 2026-08-16]",
    "src/routes/cortex.ts:GET:/v1/cortex": "DEBT medium, wants catalogue:read — An app grant with any one scope enumerates every cortex extension installed on the node, private ones included, by choosing its own ?visibility=. On a node with anonymous mode enabled, so does anyone at all, via the /v1/auth/anonymous token — requireAuth() only refuses the INJECTED anonymous identi… [triaged 2026-08-16]",
    "src/routes/cortex.ts:GET:/v1/cortex/:name": "DEBT medium, wants cortex:write — An app grant with any one scope reads the full component map and activation artifacts of any cortex on the node, including a private one belonging to another owner. cortex:write is not in APP_GRANTABLE_SCOPES (routes/app-grants.ts:86-160), so the gate this route is missing is one an app could never… [triaged 2026-08-16]",
    "src/routes/cortex.ts:GET:/v1/cortex/:name/prompts": "DEBT low, wants cortex:write — Any app grant enumerates the prompt names and variable lists of any cortex on the node, private or not, on any single scope. It is the index for the next route, which hands over the text itself. [triaged 2026-08-16]",
    "src/routes/cortex.ts:GET:/v1/cortex/:name/prompts/:promptName": "DEBT medium, wants cortex:write — An app grant holding one unrelated scope reads the entire prompt text of any cortex on the node — another owner's private cortex included — rendered with the app's own owner name and GAII pasted in. [triaged 2026-08-16]",
    "src/routes/cortex.ts:GET:/v1/cortex/:name/ontology": "DEBT low, wants cortex:write — Any app grant reads the ontology (concept vocabulary) of any cortex on the node, another owner's private one included, holding whatever single scope its consent screen showed. [triaged 2026-08-16]",
    "src/routes/ecosystem-apps.ts:POST:/v1/ecosystem-apps/hello": "OPEN BY DESIGN — Device-authorization step: there is by definition no session yet. Abuse is bounded by the 10-pending-per-owner cap (line 117), the 30-minute expiry, and the fact that only the owner-gated approve route (line 298, with the ownerName check at 307) mints a credential. [triaged 2026-08-16]",
    "src/routes/ecosystem-apps.ts:POST:/v1/ecosystem-apps/token": "OPEN BY DESIGN — RFC 8628 token polling: the credential being collected is the caller's first, so a session gate is impossible. The gate is the 32-byte device_code, one-time pickup (line 256), a 30-minute expiry and poll-interval backoff. [triaged 2026-08-16]",
    "src/routes/exchange-market.ts:POST:/v1/exchange/offerings": "DEBT high, wants exchange:write — With any single approved scope it publishes priced offerings on the open marketplace under the owner's identity, carrying usage terms (`derivatives`, `resale`, `attribution`) and a provenance attestation the owner never wrote — commitments about the owner's data that outside catalogues then read as… [triaged 2026-08-16]",
    "src/routes/exchange-market.ts:GET:/v1/exchange/offerings/:id/consumers": "DEBT medium, wants exchange:read — The complete customer list of any of the owner's offerings — which accounts buy, which of their agents call it, how much each has spent and when they last called — on any single approved scope. That is other owners' identities and usage patterns. The MCP tool for the same read, `aimeat_exchange_con… [triaged 2026-08-16]",
    "src/routes/exchange-market.ts:DELETE:/v1/exchange/offerings/:id": "DEBT high, wants exchange:write — With any single approved scope it can take down any of the owner's live marketplace listings — and `?force=1` (:467) overrides the SOURCE_MANAGED refusal that protects a projected listing, so even the auto-managed ones go. A published, priced surface other accounts hold contracts against disappears… [triaged 2026-08-16]",
    "src/routes/exchange-market.ts:POST:/v1/exchange/reconcile": "DEBT medium, wants exchange:write — It can re-project the owner's marketplace listings against their sources — which lists and delists live offerings — and with `migrate: true` adopt hand-authored listings into the projection model, changing whether a later delete is refused as SOURCE_MANAGED (:467). All on any single approved scope. [triaged 2026-08-16]",
    "src/routes/exchange-market.ts:POST:/v1/exchange/needs": "DEBT medium, wants exchange:write — It publishes demand records on the open marketplace under the owner's identity — description, app binding, usage intent, budget unit and cap, and an `autonomy: 'auto'` flag (:522) that tells the market this owner accepts matches without supervision — on any single approved scope. `aimeat_exchange_n… [triaged 2026-08-16]",
    "src/routes/exchange-market.ts:POST:/v1/exchange/needs/:id/close": "DEBT low, wants exchange:write — It can close any of the owner's own open needs on any single approved scope, withdrawing them from the market. Bounded to the owner's own records and reversible by re-posting, which is what keeps this the smallest item in the slice. [triaged 2026-08-16]",
    "src/routes/exchange-market.ts:POST:/v1/exchange/needs/:id/bids": "DEBT medium, wants exchange:write — It can post public bids in the owner's name against anyone's open need, offering any action the owner's own extensions expose, on any single approved scope. It cannot bid with a foreign extension (:560), but nothing asks whether it may transact on the market at all. MCP gates `aimeat_exchange_bid`… [triaged 2026-08-16]",
    "src/routes/exchange-market.ts:POST:/v1/exchange/needs/:id/bids/:bidId/accept": "DEBT high, wants exchange:write — With any single approved scope it mints a durable metered entitlement in the owner's name — a standing right to be charged per call at the provider's price, with the ceiling taken from the request body's `cap_units` or the need's own budget. Note the asymmetry this exposes: `contract:spend` gates D… [triaged 2026-08-16]",
    "src/routes/exchange.ts:POST:/v1/exchange/entitlements": "DEBT high, wants exchange:write — It can sign a contract in the owner's name — the exact door invariant 15 describes, enforced on the MCP surface and open here. contract:spend (metered-access.ts:50, 213-232) stops an app from DRAWING on a contract at call time; nothing stops it from creating one, at whatever budget cap it chooses… [triaged 2026-08-16]",
    "src/routes/exchange.ts:GET:/v1/exchange/grants": "DEBT medium, wants exchange:read — The whole guest list. req.auth!.owner holds the human's account name on an app grant exactly as on an owner session, so an app with any single approved scope reads who its owner is carrying, why (reason.app_id/role), and what that has cost them. exchange:grant, which the write twins require, is in… [triaged 2026-08-16]",
    "src/routes/exchange.ts:GET:/v1/exchange/entitlements": "DEBT medium, wants exchange:read — Full read of the owner's contract book. An app grant is roles:['app'] with sub = the owner's GHII (app-grants.ts:522, gaii: ownerGhii at :633), so resolveIdentity (gaii.ts:145-149) returns the owner GHII and the list matches the owner's own contracts. requireAuth() asks nothing about scopes, so an… [triaged 2026-08-16]",
    "src/routes/exchange.ts:POST:/v1/exchange/entitlements/off": "DEBT medium, wants exchange:write — It can switch the owner's contracts off, including `mode:'revoke'`, which is terminal for that record. The 404 at :426 is satisfied because the app grant carries the owner's name; no scope is consulted. Consequence is denial rather than theft: a paid capability the owner depends on stops being serv… [triaged 2026-08-16]",
    "src/routes/exchange.ts:POST:/v1/exchange/proposals": "DEBT medium, wants exchange:write — It can open a renegotiation on any contract its owner is party to and message the counterparty as them. Nothing moves until the counterparty accepts, so the money exposure is second-order; the identity exposure (a message sent under the human's name, with skipContactGate) is immediate. [triaged 2026-08-16]",
    "src/routes/exchange.ts:GET:/v1/exchange/proposals": "DEBT medium, wants exchange:read — Every live negotiation the owner is in, incoming and outgoing, with counterparty names and both sides' prices. The filter reads req.auth!.owner, which an app grant carries, and nothing asks which scope. [triaged 2026-08-16]",
    "src/routes/exchange.ts:POST:/v1/exchange/proposals/:id/accept": "DEBT high, wants exchange:write — It can accept a price increase for the owner. The counterparty check passes on the owner's name, which the app grant carries, and no scope is asked — so a proposal raising price_per_call becomes the live contract every one of that owner's principals then spends against. This is the sharpest money d… [triaged 2026-08-16]",
    "src/routes/exchange.ts:POST:/v1/exchange/proposals/:id/decline": "DEBT low, wants exchange:write — It can decline a change the human was still considering, and tell the counterparty so under the owner's name. The owner-name check admits it; no scope is consulted. Nothing financial moves, so the loss is the negotiation and the message. [triaged 2026-08-16]",
    "src/routes/exchange.ts:POST:/v1/exchange/proposals/:id/withdraw": "DEBT low, wants exchange:write — It can withdraw its owner's own pending proposal — the check at :607 passes on the owner name it carries, and no scope is asked. The narrowest of the four proposal doors: one pending negotiation of the owner's own disappears. [triaged 2026-08-16]",
    "src/routes/exchange.ts:GET:/v1/exchange/entitlements/history": "DEBT medium, wants exchange:read — Full read of the owner's past contracts — the same money record as the live list, one negotiation older. sub = owner GHII, so the filter matches; whatever single scope the consent screen displayed is enough. [triaged 2026-08-16]",
    "src/routes/exchange.ts:GET:/v1/exchange/provider/history": "DEBT medium, wants exchange:read — The owner's revenue history, buyer by buyer. An app grant's sub is the owner GHII, so resolveIdentity produces the provider key and the filter matches. The neighbouring earnings route refuses an app without wallet:read; this one refuses nothing. [triaged 2026-08-16]",
    "src/routes/exchange.ts:POST:/v1/exchange/work": "DEBT high, wants exchange:write — It can queue work that will charge its owner when the provider delivers, and the contract:spend permission cannot see that charge because settlement happens on the provider's request with no consumer session. So this is the one door where an app holding no spend permission still causes the owner's… [triaged 2026-08-16]",
    "src/routes/exchange.ts:POST:/v1/exchange/work/:id/deliver": "DEBT high, wants exchange:write — It can declare its owner's work delivered and thereby charge a THIRD PARTY. The 404 at :741 only asks whether the work belongs to this owner, which the app grant satisfies; no scope is asked. An app with one unrelated scope can mark open work delivered with arbitrary output and move another owner's… [triaged 2026-08-16]",
    "src/routes/exchange.ts:GET:/v1/exchange/work": "DEBT medium, wants exchange:read — Every agent-work item in both directions, payloads included. The owner name on the app-grant token satisfies the filter, so an app approved for one unrelated scope reads the delivered content the owner paid for and the tasks the owner's agent was paid to do. [triaged 2026-08-16]",
    "src/routes/extensions/actions.ts:POST:/v1/ext/:extName/:instanceId/:actionId": "DEBT high, wants ext:invoke — Same as the default-instance door, one tenant deeper: an app grant with any single scope runs any action of any active instance, writing into that instance's ext:{name}.{instanceId} namespace. Only the charging paths ask it for contract:spend (services/metered-access.ts:217-224). [triaged 2026-08-16]",
    "src/routes/extensions/actions.ts:POST:/v1/ext/:extName/:actionId": "DEBT high, wants ext:invoke — An app grant holding one unrelated scope (storage:read, say) executes any action of any active extension on the node, in that extension's own ext:{name} namespace, with a ctx that can burn the caller's morsels, send email and notifications, write files and call external APIs (buildExtensionCtx at :… [triaged 2026-08-16]",
    "src/routes/extensions/crud.ts:POST:/v1/extensions": "GATED IN THE HANDLER OR THE SERVICE — Gated in the handler: hasExtWritePermission (extensions/permissions.ts:17-26) requires the operator role, the configured install role, or ext:write — checked before the presigned token and before the record is written. [triaged 2026-08-16]",
    "src/routes/extensions/crud.ts:PUT:/v1/extensions/:name": "GATED IN THE HANDLER OR THE SERVICE — Gated in the handler: create takes hasExtWritePermission and update takes canManageInstalledExt (extensions/crud.ts:182-191, rules in extensions/permissions.ts:17-62), both before the record is built or written. [triaged 2026-08-16]",
    "src/routes/extensions/crud.ts:PATCH:/v1/extensions/:name/actions/:actionId": "GATED IN THE HANDLER OR THE SERVICE — Gated in the handler: canManageInstalledExt (extensions/permissions.ts:53-62) refuses anyone but the installing owner or an operator, before the script is written. [triaged 2026-08-16]",
    "src/routes/extensions/crud.ts:DELETE:/v1/extensions/:name": "GATED IN THE HANDLER OR THE SERVICE — Gated in the handler: canManageInstalledExt (extensions/permissions.ts:53-62) requires installedBy === caller.owner plus the owner role or ext:write, checked before uninstallExtension runs. [triaged 2026-08-16]",
    "src/routes/extensions/instances.ts:GET:/v1/extensions/:name/instances": "DEBT medium, wants ext:write — An app grant with any single scope lists the instances and instance config of any extension on the node, including another owner's. Nothing compares owners, so the token's scopes never enter the answer; only manifest-declared secret fields are masked. [triaged 2026-08-16]",
    "src/routes/extensions/instances.ts:GET:/v1/extensions/:name/instances/:instanceId": "DEBT medium, wants ext:write — An app grant on any single scope reads any named instance of any extension, another owner's included. The scope list is never consulted, and an instance config field the manifest author did not mark `type: secret` is returned verbatim. [triaged 2026-08-16]",
    "src/routes/flags.ts:POST:/v1/flags": "DEBT medium, wants social:write — It can file one report per target in its owner's name. Being able to report is the design: the node's own transparency statement publishes POST /v1/flags as the correction procedure anyone may use (src/routes/ai-transparency.ts:156-161). What it cannot do is hide content on its own — the weighted t… [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/flags.ts:PUT:/v1/flags/:id": "GATED IN THE HANDLER OR THE SERVICE — src/routes/flags.ts:112 — router.put('/v1/flags/:id', requireAuth(), ...) with no gate middleware; the handler resolves isOperator (129) and isOrganismAdmin (131-139, via resolveOrganismForFlag plus `organism.admins.includes(ghiiRecord.ghii)` where ghiiRecord = storage.getGHIIByOwner(req.auth!.owne… [triaged 2026-08-16]",
    "src/routes/ghii/attach-email.ts:POST:/v1/ghii/login/attach-email": "OPEN BY DESIGN — A login-flow step for an account that cannot hold a session yet: it re-verifies username and password itself, under the login rate limit and the per-account lockout, and refuses any account already past email verification. [triaged 2026-08-16]",
    "src/routes/ghii/profile.ts:GET:/v1/ghii/list": "DEBT low, wants catalogue:read — The member directory. An app approved for memory:read alone browses every opted-in member of the node, which is a read of other owners' profiles rather than of its own owner's. 'catalogue:read' is already the word for it: it is in APP_GRANTABLE_SCOPES (src/routes/app-grants.ts:92) described as 'Rea… [triaged 2026-08-16]",
    "src/routes/ghii/profile.ts:GET:/v1/ghii/cors": "DEBT low, wants account:security — The account's CORS allowlist, on any single approved scope. Read-only, so it is reconnaissance rather than escalation: it tells a third-party app which web origins already hold credentialed access to this account. [triaged 2026-08-16]",
    "src/routes/ghii/profile.ts:GET:/v1/ghii/me": "DEBT medium, wants account:security — The account's recovery address, whether a password is set at all, and the email-verification state, on any single approved scope. That is the reconnaissance half of the H-5 takeover path; it changes nothing by itself, which is why this is medium rather than high. Note before tightening: requireScop… [triaged 2026-08-16]",
    "src/routes/ghii/profile.ts:PUT:/v1/ghii": "DEBT high, wants memory:write-reserved — Rewrite the person's public identity and damage their sign-in. An app approved for memory:read alone can change display_name, bio, avatar and locale, repoint notification_email (which clears emailVerifiedAt and sets magicLinkEnabled false, so the person's verified address and magic-link login are g… [triaged 2026-08-16]",
    "src/routes/ghii/recovery.ts:POST:/v1/ghii/password/reset-request": "OPEN BY DESIGN — Starts a password reset for a caller who by definition has no session. Mails only to an already-verified address, answers identically whether the account exists, and is capped at 3 per 10 minutes. [triaged 2026-08-16]",
    "src/routes/ghii/recovery.ts:POST:/v1/ghii/password/reset": "OPEN BY DESIGN — Password reset by emailed code: unauthenticated by definition, since a locked-out person has no session. The code, its 10-minute expiry, the 5-attempt cap and the 3-per-10-minutes limit are the gate, and all four are checked before the password is written. [triaged 2026-08-16]",
    "src/routes/ghii/recovery.ts:POST:/v1/ghii/account/recover": "OPEN BY DESIGN — Account-recovery mail, unauthenticated by definition: the caller has no session. The handler reads no principal, mails only the address already on the record, answers identically for a hit and a miss, and is capped at 3 per 10 minutes. [triaged 2026-08-16]",
    "src/routes/ghii/register-login.ts:POST:/v1/ghii": "OPEN BY DESIGN — Public registration: the caller has no identity yet, so there is no principal to scope. Rate-limited, schema-validated, and refuses an existing username or an email already in use. [triaged 2026-08-16]",
    "src/routes/ghii/register-login.ts:POST:/v1/ghii/login": "OPEN BY DESIGN — src/routes/ghii/register-login.ts:314 — chain is `rateLimit({ max: config.loginRateLimitMax, windowMs: config.loginRateLimitWindowMs }), validateBody(GhiiLoginSchema, config.nodeId)`. This is the login itself, so a gate is impossible by definition. The decision is the password: `verifyPassword(pass… [triaged 2026-08-16]",
    "src/routes/ghii/web-verify.ts:POST:/v1/ghii/register-web": "OPEN BY DESIGN — Public web registration: the caller has no identity yet, so there is no principal to scope. Rate-limited, refuses a taken username or an email already in use, and claims no verified-email binding until the code is confirmed. [triaged 2026-08-16]",
    "src/routes/ghii/web-verify.ts:POST:/v1/ghii/verify-email": "OPEN BY DESIGN — Confirms an emailed registration code for a person who has no session yet. Two secrets are required (the 128-bit verification id and the code), attempts are capped at 5, and the token it mints is roles ['agent'] with the agent's own scopes. [triaged 2026-08-16]",
    "src/routes/ghii/web-verify.ts:POST:/v1/ghii/magic-link": "OPEN BY DESIGN — Requests a magic link for a caller who by definition has no session. Mails only to an address already verified on an account, reveals nothing in the response, and the emailed token is single-use with a 15-minute expiry. [triaged 2026-08-16]",
    "src/routes/instances/manage.ts:GET:/v1/instances": "GATED IN THE HANDLER OR THE SERVICE — The listing is keyed on the caller's own `req.auth!.owner` (manage.ts:43, passed as the query key at :50), so no principal can name another owner's instances and the response is the caller's own install inventory. [triaged 2026-08-16]",
    "src/routes/instances/manage.ts:GET:/v1/instances/:id/status": "GATED IN THE HANDLER OR THE SERVICE — The handler refuses a non-owner in place: manage.ts:71 `if (instance.owner !== owner) → 403`; the hash comparison and the customization write at :99 only ever touch that owner's own instance record. [triaged 2026-08-16]",
    "src/routes/instances/manage.ts:GET:/v1/instances/:id/check-update": "GATED IN THE HANDLER OR THE SERVICE — The handler refuses a non-owner in place: manage.ts:133 `if (instance.owner !== owner) → 403`, and the diff is built only from that instance and the package's own published versions. [triaged 2026-08-16]",
    "src/routes/instances/manage.ts:GET:/v1/instances/:id": "GATED IN THE HANDLER OR THE SERVICE — The handler refuses a non-owner in place: manage.ts:242 `if (instance.owner !== owner && !roles.includes('operator')) → 403`, so the record is only ever returned to its own owner or to the node operator. [triaged 2026-08-16]",
    "src/routes/instances/manage.ts:DELETE:/v1/instances/:id": "DEBT high, wants packages:write — Removes any of the owner's instances, and with `removeComponents` it unregisters every app, cortex, extension and scheduled job that install created under the owner's identity. :264 compares the human's account name, which an app grant carries, so it passes. [triaged 2026-08-16]",
    "src/routes/instances/migration.ts:POST:/v1/instances/:id/migration-prompt": "GATED IN THE HANDLER OR THE SERVICE — Read-only and owner-checked in place: migration.ts:44 `if (instance.owner !== owner) → 403`, after which the handler only assembles prompt text from that owner's own instance and the package versions it names. [triaged 2026-08-16]",
    "src/routes/instances/migration.ts:POST:/v1/instances/:id/apply-migration": "DEBT high, wants packages:write — The install door by another name. An app grant passes :180 because it carries the human's account name, and can then replace the owner's registered apps, cortexes and extensions with body-supplied source. Its sibling, POST /v1/packages/:groupId/install, asks for `packages:write` (install.ts:83); th… [triaged 2026-08-16]",
    "src/routes/invite-accept.ts:POST:/v1/invitations/:token/accept": "DEBT high, wants requireOwnerPrincipal() on the authenticated branch — no scope word in the vocabulary covers minting a session; account:security (ACCOUNT_SECURITY_SCOPE) is the vocabulary this door belongs to, and it is deliberately absent from APP_GRANTABLE_SCOPES — An app grant satisfies `req.auth && req.auth.anonymous !== true` (133), so it accepts AS the owner: it can join the owner to an organism and take the inviter's chosen org role and workspace grants with no scope word. Two things bound that — it must hold the invitation token from the emailed link, a… [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/knowledge/admin.ts:GET:/v1/knowledge/:id/reviews": "DEBT medium, wants knowledge:read — An app-grant token (roles ['app'], app-grants.ts:522) holding any one scope reads the operator's moderation record for ANY package id on the node, including packages owned by other people. Nothing in the chain or the handler asks who is calling. [triaged 2026-08-16]",
    "src/routes/knowledge/organism.ts:GET:/v1/knowledge/organism/:id": "DEBT medium, wants memory:read — An app grant passes as the human: req.auth.owner carries the account name on app grants, so an app holding any one scope lists every knowledge package any member contributed to any organism its owner belongs to (other owners' manifests included). The membership check refuses a different PERSON; it… [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/knowledge/packages-core.ts:POST:/v1/knowledge/import": "DEBT high, wants memory:write — An app grant carrying any single scope writes arbitrary memory records under the owner's identity, may set them public and catalog_listed (which posts a feed entry naming the owner), and may hand an organism a standing consent over `packages/{id}/*` — all reach that the memory door itself (requireS… [triaged 2026-08-16]",
    "src/routes/knowledge/sharing.ts:POST:/v1/knowledge/:id/clone": "DEBT low, wants memory:write — An app grant holding any one scope creates one memory record per cloned entry plus a manifest under the owner's identity, spending the owner's key budget. Nothing private leaks (source is public-only), but the writes are memory:write reach the grant never asked for. [triaged 2026-08-16]",
    "src/routes/knowledge/templates.ts:GET:/v1/templates/knowledge-packager-human": "OPEN BY DESIGN — Serves a node-wide system prompt with the caller's own name substituted in; the identical text is public and unauthenticated at GET /v1/portal/prompts/:promptId (prompts.ts:720). [triaged 2026-08-16]",
    "src/routes/knowledge/templates.ts:GET:/v1/templates/knowledge-packager-agent": "OPEN BY DESIGN — Serves a node-wide system prompt with the caller's own name and GAII substituted in; the identical text is public and unauthenticated at GET /v1/portal/prompts/:promptId (prompts.ts:720). [triaged 2026-08-16]",
    "src/routes/knowledge/templates.ts:GET:/v1/templates/chat-session-human": "OPEN BY DESIGN — Serves a node-wide system prompt with the caller's own name substituted in; the identical text is public and unauthenticated at GET /v1/portal/prompts/:promptId (prompts.ts:720). requireAuth is there to name the caller, not to protect anything. [triaged 2026-08-16]",
    "src/routes/knowledge/templates.ts:GET:/v1/templates/chat-session-quick": "OPEN BY DESIGN — Serves a node-wide system prompt with the caller's own name substituted in; the identical text is public and unauthenticated at GET /v1/portal/prompts/:promptId (prompts.ts:720). [triaged 2026-08-16]",
    "src/routes/ledger.ts:GET:/v1/ledger/usage": "DEBT medium, wants wallet:read — The owner's whole LLM cost history, groupable by agent, model, provider, organism or workspace. req.auth!.owner is populated on an app grant, so an app holding one unrelated scope gets it; nothing narrows the window either. [triaged 2026-08-16]",
    "src/routes/ledger.ts:GET:/v1/ledger/usage/runs": "DEBT medium, wants wallet:read — Run-level evidence of what the owner's agents did and what each run cost, with run ids that address a specific deliverable. Reachable by any app grant, and by any agent whatever its scopes, because no scope is read. [triaged 2026-08-16]",
    "src/routes/ledger.ts:GET:/v1/ledger/usage/overview": "DEBT medium, wants wallet:read — Model-grouped cost, grand totals and the per-run drill in a single response, for the owner behind the token. requireAuth() is the whole chain, so any app grant qualifies whatever it was approved for. [triaged 2026-08-16]",
    "src/routes/ledger.ts:GET:/v1/ledger/usage/capabilities": "DEBT medium, wants wallet:read — Per-capability cost plus the list of other owners who invoked them. An app grant reaches it on requireAuth alone, so a customer list that belongs to the seller leaves through whatever app the owner once connected for something else. [triaged 2026-08-16]",
    "src/routes/ledger.ts:GET:/v1/ledger/budget": "DEBT medium, wants wallet:read — The owner's spend against budget, broken down per agent. Being reachable by an app grant is intended (header :6-8, for AGENCY), but requireAuth cannot express 'the app the owner gave a usage permission to' — it admits every app grant holding any word at all, plus every scoped agent. The word is mis… [triaged 2026-08-16]",
    "src/routes/librarian.ts:GET:/v1/librarian/search": "DEBT high, wants memory:read — An app grant that was never given memory:read still gets the owner's private memory full-text searched: resolveIdentity returns the owner's GHII for role 'app' (utils/gaii.ts:145-148, because the token's sub IS the owner GHII), so viewerGaii is the person, and snippets of their private records come… [triaged 2026-08-16]",
    "src/routes/librarian.ts:POST:/v1/librarian/classify": "GATED IN THE HANDLER OR THE SERVICE — Gated in the handler: gateOwnerOrAiUse (routes/librarian.ts:47-59, called at :110) admits an owner principal or a token carrying ai:use, mirroring requireScope's owner branch, before the owner's AI key is spent. [triaged 2026-08-16]",
    "src/routes/librarian.ts:POST:/v1/librarian/plan": "GATED IN THE HANDLER OR THE SERVICE — Gated in the handler: gateOwnerOrAiUse (routes/librarian.ts:47-59, called at :141) requires an owner principal or ai:use before the owner's AI key is spent. [triaged 2026-08-16]",
    "src/routes/librarian.ts:POST:/v1/librarian/distribute": "GATED IN THE HANDLER OR THE SERVICE — Gated in the handler: gateOwnerOrAiUse (routes/librarian.ts:47-59, called at :172) requires an owner principal or ai:use before the owner's AI key is spent. [triaged 2026-08-16]",
    "src/routes/matches.ts:GET:/v1/matches": "DEBT medium, wants social:read (the word already gating the social reads on this node — src/routes/boards.ts:460 and 503) — The owner's whole match list and, for every partner who still consents to matching, that partner's ghii, displayName, sharedInterests and distanceKm (110-129). Another person's identity and rough location, handed to an app grant holding any single approved scope. [triaged 2026-08-16]",
    "src/routes/matches.ts:POST:/v1/matches/:id/respond": "GATED IN THE HANDLER OR THE SERVICE — src/routes/matches.ts:180 — router.post('/v1/matches/:id/respond', requireAuth(), ...) with no gate middleware; the handler resolves callerGhii from req.auth!.owner via storage.getGHIIByOwner (197-207) and refuses at 209-212: `if (match.profileA !== callerGhii && match.profileB !== callerGhii) { 40… [triaged 2026-08-16]",
    "src/routes/memory/bulk.ts:GET:/v1/memory/discover": "OPEN BY DESIGN — Cross-owner browse of `visibility: 'public'` rows only, returning metadata without values (bulk.ts:413, 432-440): the writer's own visibility choice is the access decision, and the doors that return values are scope-gated. [triaged 2026-08-16]",
    "src/routes/notifications.ts:GET:/v1/notifications": "DEBT medium, wants memory:read (notifications are memory records under NOTIF_PREFIX in the owner GHII's namespace; memory:read is the word that fences an app grant on the same rows) — The owner's 50 most recent notifications with title, body, link and unread count — which includes previews of messages, work offers and moderation notices written by OTHER owners. Reached with any single approved scope, while the same rows read through /v1/memory require memory:read. [triaged 2026-08-16]",
    "src/routes/notifications.ts:POST:/v1/notifications/read": "DEBT low, wants memory:write (the write is storage.setMemory over the owner's own NOTIF_PREFIX records, which is what memory:write gates everywhere else) — It can mark every one of the owner's notifications read, which is enough to silence the bell on something the human never saw. Self-targeted only (line 141), so nothing cross-owner, and no scope word is asked for the write. [triaged 2026-08-16]",
    "src/routes/notifications.ts:DELETE:/v1/notifications": "DEBT medium, wants memory:delete (a notification is a memory record under NOTIF_PREFIX in the owner's namespace, and memory:delete is the word that fences an app grant on those same rows) — It can clear the owner's entire notification inbox, every row under NOTIF_PREFIX in the owner GHII's namespace, holding whatever single scope was approved. The same rows deleted through /v1/memory need memory:delete; here the bell just goes empty, and the sibling POST /v1/notifications on line 66 a… [triaged 2026-08-16]",
    "src/routes/oauth-login.ts:POST:/v1/ghii/login/${p.id}/finalize": "OPEN BY DESIGN — Completes a brand-new social signup, which by definition has no session. The credential is the node-signed, purpose-stamped pending cookie verified at line 424; a duplicate email is refused 409 and a taken username 409 before anything is created. [triaged 2026-08-16]",
    "src/routes/organisms/gates.ts:POST:/v1/organisms/:id/approvals": "DEBT medium, wants organism:write — An app grant with any single scope files approval requests in any organism its owner belongs to, including one whose `action` is 'publish' with arguments of its choosing and a `prompt` string the human reads before approving. It cannot resolve its own request: the resolve door is requireRole('owner… [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/organisms/gates.ts:GET:/v1/organisms/:id/approvals": "DEBT low, wants organism:read — An app grant rides the owner's membership: holding any one scope it reads the approval inbox (actor, action, arguments, prompt text, deadlines) of every organism its owner belongs to. The membership test refuses another person, never a narrower principal. [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/organisms/gates.ts:POST:/v1/organisms/:id/publish": "DEBT high, wants organism:write — An app grant carrying any single scope (and any scope-limited agent) publishes any existing draft in any workspace of any organism its owner belongs to — including a draft another member wrote — making it live and logging the decision in the publisher's name. Membership does not fence it: the grant… [triaged 2026-08-16]",
    "src/routes/organisms/gates.ts:POST:/v1/organisms/:id/revert": "DEBT medium, wants organism:write — An app grant with any single scope copies the full value of any published record it can read into a draft under the owner's identity, and blocks every other member from reopening that record until the draft is dealt with (DRAFT_EXISTS, shared.ts:383). The read side is gated by canReadWorkspace; the… [triaged 2026-08-16]",
    "src/routes/organisms/intake.ts:POST:/v1/intake/forms": "DEBT high, wants organism:write — An app grant carrying any single scope defines a NEW public, unauthenticated write endpoint into a workspace its owner created or admins, choosing the destination namespace, the accepted fields and whether submissions publish straight to .latest. The door outlives the grant: revoking the app does n… [triaged 2026-08-16]",
    "src/routes/organisms/intake.ts:GET:/v1/intake/forms": "GATED IN THE HANDLER OR THE SERVICE — Only the workspace creator or an org admin passes requireWsOwner (intake.ts:87-99, called at line 158), and the response is limited to that workspace's own intake configs — a read of the caller's own workspace settings. [triaged 2026-08-16]",
    "src/routes/organisms/intake.ts:DELETE:/v1/intake/forms": "DEBT medium, wants organism:write — An app grant holding any one scope deletes any intake form in a workspace its owner created or admins, which kills a live public submission link (lead capture, feedback, RSVP) without any word being granted for it. The creator/admin test refuses another person, not a narrower principal. [triaged 2026-08-16]",
    "src/routes/organisms/intake.ts:POST:/v1/intake/:org/:ws/:formId": "OPEN BY DESIGN — intake.ts:199-201 `router.post('/v1/intake/:org/:ws/:formId', rateLimit({ windowMs: 15 * 60_000, max: 20, keyBy: 'ip' }), async (req, res) => {` — no auth by design (file header, intake.ts:18). What stops abuse instead: per-IP rate limit (200), honeypot silently discarding bots (208-210), field-cou… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-access.ts:GET:/v1/organisms/:id/workspaces": "GATED IN THE HANDLER OR THE SERVICE — Membership (line 91) plus a per-workspace read-authorization probe (114-124, canReadWsManifest / authorizeRead) decide this; discovery is open to members by design while content stays behind requireScope('organism:read') at workspace-ops.ts:436. [triaged 2026-08-16]",
    "src/routes/organisms/workspace-access.ts:GET:/v1/organisms/:id/workspace-access": "GATED IN THE HANDLER OR THE SERVICE — Membership plus the workspace-creator/org-admin test decide this in the handler (workspace-access.ts:284-285 and 326-328), and the ?all=1 branch filters rows the same way at line 303 — a manager's read of workspaces they manage. [triaged 2026-08-16]",
    "src/routes/organisms/workspace-access.ts:POST:/v1/organisms/:id/workspace-access/grant": "DEBT high, wants organism:invite — An app grant carrying any single scope adds any account as viewer or contributor to a workspace its owner created or admins — a standing read of other members' content, written on the creator's behalf — and the recipient keeps that access after the grant is revoked. [triaged 2026-08-16]",
    "src/routes/organisms/workspace-access.ts:POST:/v1/organisms/:id/workspace-access/revoke": "DEBT medium, wants organism:invite — An app grant carrying any single scope cuts any member off a workspace its owner created or admins. Same door and same missing word as the grant twin; requireWsManager refuses a different person, not a narrower principal. [triaged 2026-08-16]",
    "src/routes/organisms/workspace-access.ts:POST:/v1/organisms/:id/invitations/code": "SCANNER LIMITATION, not an exemption on the merits — Gated by requireScope('organism:invite') inside the shared codeInviteGuards array (shared.ts:704) spread into the route. KEEP THIS ENTRY: the scanner only recognises gate arrays declared in the same file (check-route-scopes.ts:86-94), so deleting it turns a gated route into a new un-exempt finding. The gate reads middleware arrays declared in the same file only (check-route-scopes.ts:86-94); this one lives in shared.ts:704. [triaged 2026-08-16]",
    "src/routes/organisms/workspace-access.ts:POST:/v1/organisms/:id/invitations/code/:invId/cancel": "SCANNER LIMITATION, not an exemption on the merits — Gated by requireScope('organism:invite') inside the shared codeInviteGuards array (shared.ts:704), plus an inviter-or-admin test at line 685. KEEP THIS ENTRY: the scanner cannot see a gate array declared in another file (check-route-scopes.ts:86-94), so deleting it creates a new un-exempt finding. The gate reads middleware arrays declared in the same file only (check-route-scopes.ts:86-94); this one lives in shared.ts:704. [triaged 2026-08-16]",
    "src/routes/organisms/workspace-ops.ts:POST:/v1/organisms/:id/workspace/engagements": "DEBT medium, wants organism:write — It can adopt a contract engagement for any of the owner's agents in any workspace the owner belongs to. The own-agent test compares against `req.auth!.owner`, which the app-grant token carries, so it is satisfied by construction. An app grant holding one unrelated scope writes shared organism state… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-ops.ts:POST:/v1/organisms/:id/workspace/engagements/retire": "DEBT high, wants organism:write — The off-switch, including on somebody else's agent. Both branches key on `req.auth!.owner`, which an app-grant token carries, so an app grant with one unrelated scope can retire the owner's own agents' engagements and — when the owner created the workspace or is an org creator/admin — ANOTHER owner… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-ops.ts:GET:/v1/organisms/:id/workspace/engagements": "DEBT medium, wants organism:read — The full engagement list of any workspace in an organism the owner belongs to. memberRole() keys on `req.auth!.owner`, which an app grant carries, and nothing after :114 filters by principal — so an app grant with one unrelated scope reads which agents (including other owners' agents, by GAII) are… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-ops.ts:GET:/v1/organisms/:id/workspace/activity": "DEBT medium, wants organism:read — Full read of the workspace's edit/publish history. Both the membership test and callerGaii resolve to the human's own account for an app grant (sub = owner GHII), so the record-level authorizeRead never narrows anything: an app grant with one unrelated scope sees who wrote and published what, and w… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-ops.ts:GET:/v1/organisms/:id/agents/activity": "DEBT medium, wants organism:read — Full read. app-grants.ts:522 mints `roles:['app']`, `sub = owner GHII`; requireAuth() admits it, memberRole() reads `req.auth!.owner` (the human's account name) and resolveIdentity returns that same owner GHII, so every authorizeRead at :206 passes as the human. An app grant holding one unrelated s… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-ops.ts:GET:/v1/organisms/:id/workspace/participants": "DEBT high, wants organism:read — The whole participant hierarchy: every node, every owner name, whether each is a member/creator, and every agent name with its contribution count. Membership is tested on `req.auth!.owner`, so an app grant holding one unrelated scope reads the identities of every human and agent that has touched th… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-ops.ts:POST:/v1/organisms/:id/workspace/share/unlock": "OPEN BY DESIGN — Public by design: the anonymous door that trades a share password for a short-lived, org+ws-bound share token, so there is no session to gate. Abuse is bounded by rateLimit(15 min / 10 per IP), a timing-uniform dummy-hash verify that never discloses whether password mode is configured, and a generi… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-ops.ts:GET:/v1/organisms/:id/workspace/export": "DEBT high, wants organism:read — A full workspace ZIP whenever the owner is the workspace creator or an org creator/admin. The creator test compares `createdBy` against `req.auth!.owner` — the account name an app-grant token carries — so it refuses a different PERSON and admits an app acting in this person's name. An app grant wit… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-ops.ts:POST:/v1/organisms/:id/workspace/import": "DEBT high, wants organism:write — It can create a whole workspace in the organism from a 64 MB ZIP. memberRole() keys on `req.auth!.owner`, so the app grant passes as the human, and the two identities handed to importWorkspace at :610 are the human's own. The same act performed through POST /v1/organisms/:id/workspaces is refused t… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-ops.ts:GET:/v1/organisms/:id/export": "DEBT high, wants organism:read — A complete organism ZIP. The membership row is looked up by `req.auth!.owner`, which an app-grant token carries verbatim, and the exporter identity is rebuilt from that same name at :637 — so an app grant holding any single scope downloads every workspace of the organism, including records written… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-read.ts:GET:/v1/organisms/:id/workspace": "DEBT high, wants organism:read — Every record and document of the workspace: manifest, readme, pinned apps, all published objects, all drafts, decisions, resources and linked todos, with `_aiProvenance` attached. callerGaii is the owner GHII on an app grant, so `manRec.ownerGaii === callerGaii` at :122 is true for the owner's own… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-read.ts:GET:/v1/organisms/:id/overview": "DEBT low, wants organism:read — The whole-organism overview: workspace names, spaces and per-space counts, archived ones included on request. The membership test reads `req.auth!.owner`, so an app grant with one unrelated scope passes as the human. Counts and names rather than record values, which is why this is the low end of th… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-read.ts:GET:/v1/organisms/:id/instruction-block": "DEBT low, wants organism:read — The generated instruction block, which names the organism, every active workspace, its spaces and a readme snippet. Membership is keyed on `req.auth!.owner`, so an app grant holding one unrelated scope reads the organism's structural map. Thin next to the content routes — names, spaces and a trunca… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-read.ts:GET:/v1/organisms/:id/workspace/overview": "DEBT medium, wants organism:read — A deep per-space listing of the workspace: record and document titles, their ids, counts, and the objectives with their computed current values. Membership is keyed on `req.auth!.owner`, so one unrelated scope reaches it — and the ids returned here are exactly what a follow-up targeted read needs. [triaged 2026-08-16]",
    "src/routes/organisms/workspace-read.ts:GET:/v1/organisms/:id/graph": "DEBT medium, wants organism:read — The organism's full structural graph, members and agents included. The membership branch that fires for an app grant is the `ownerName` one, and ownerName is `req.auth!.owner` — the human's account name — so an app grant with one unrelated scope reads the map of every workspace and space plus the i… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-read.ts:GET:/v1/organisms/:id/workspace/graph": "DEBT low, wants organism:read — The structural graph of one named workspace in any organism the owner belongs to. The membership branch keys on `req.auth!.owner`, so an app grant with one unrelated scope passes. Spaces and structure only, no record values, so this is the low end of the same missing gate. [triaged 2026-08-16]",
    "src/routes/organisms/workspace-read.ts:GET:/v1/organisms/:id/workspace/dangling-refs": "DEBT medium, wants organism:read — The referential-integrity report over every readable workspace, which means it walks the record and document CONTENT and hands back ids and prose fragments from it. Membership and callerGaii both resolve to the human, so an app grant with one unrelated scope gets it. [triaged 2026-08-16]",
    "src/routes/organisms/workspace-read.ts:GET:/v1/organisms/:id/structure/history": "DEBT medium, wants organism:read — The organism's whole structural timeline, up to 500 archived fingerprints with their `_event`/`_diff`/`_recordedAt`, plus a write: the app grant's own visit stamps a new snapshot under the caller's resolved identity at :422. Membership is keyed on `req.auth!.owner`, so one unrelated scope is enough… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-read.ts:GET:/v1/organisms/:id/search": "DEBT high, wants organism:read — Full-text search over the CONTENT of every workspace the owner can read, archived content included via `?archived=only`. Both the membership name and callerGaii are the human's own on an app grant, so an app holding one unrelated scope can dredge the organism for any string and receive matching sni… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-read.ts:GET:/v1/organisms/:id/comments": "DEBT medium, wants organism:read — Every comment thread on any record of any workspace the owner can read. canReadWorkspace takes `callerOwner = req.auth!.owner` and `callerGaii = resolveIdentity(...)`, both of which are the human's own for an app grant, and the GEAI narrowing at workspace-access.ts:105 does not apply because sub is… [triaged 2026-08-16]",
    "src/routes/organisms/workspace-read.ts:POST:/v1/organisms/:id/comments/batch": "DEBT medium, wants organism:read — Every comment thread of every requested target, in one call. The gate takes `req.auth!.owner` and the resolved callerGaii, both the human's own on an app grant, so nothing is omitted for that caller: an app grant with one unrelated scope harvests other members' comment bodies and authors across man… [triaged 2026-08-16]",
    "src/routes/owners.ts:POST:/v1/owners": "OPEN BY DESIGN — Programmatic owner registration: the caller has no identity yet, so there is no principal to scope. An extension hook, name validation and a NAME_TAKEN 409 are the controls, plus the /v1/owners path rate limiter. [triaged 2026-08-16]",
    "src/routes/packages.ts:POST:/v1/packages/import": "GATED IN THE HANDLER OR THE SERVICE — Operator-only in the handler: packages.ts:92 refuses any principal without the operator role while `packageCreateRole` is at its default 'operator' (config.ts:598), and the import always lands private (:219). [triaged 2026-08-16]",
    "src/routes/packages.ts:POST:/v1/packages": "GATED IN THE HANDLER OR THE SERVICE — Operator-only in the handler: packages.ts:275 refuses any principal without the operator role while `packageCreateRole` is at its default 'operator' (config.ts:598); if the node is ever set to 'owner' this branch becomes a no-op and the route needs a real scope. [triaged 2026-08-16]",
    "src/routes/packages.ts:POST:/v1/packages/:groupId/versions": "DEBT high, wants app:write — Publishes a new version of any package the owner authored, with caller-chosen component source, directly at status 'published'. Whoever installs that group afterwards registers those components under their own identity (instances/install.ts). The author test at :457 is an owner-name comparison an a… [triaged 2026-08-16]",
    "src/routes/packages.ts:PATCH:/v1/packages/:groupId/versions/:version": "DEBT medium, wants app:write — Promotes any draft version of the owner's packages to published, which is what makes it installable by anyone (instances/install.ts:106 accepts only `status === 'published'`), or archives a live one. :629 compares the human's account name, so an app grant passes. [triaged 2026-08-16]",
    "src/routes/packages.ts:PATCH:/v1/packages/:groupId": "DEBT high, wants app:write — Flips any of the owner's package groups from private to public, which exposes every component's stored source (the same records the three read doors at :537, :580 and :607 currently 404 to strangers), and rewrites the group's description and tags. The author test at :663 is an owner-name comparison… [triaged 2026-08-16]",
    "src/routes/packages.ts:DELETE:/v1/packages/:groupId": "DEBT medium, wants app:manage — Archives any package group the owner authored, taking it out of the gallery and the browse listings. The author test at :712 compares the human's account name, which the grant carries, so it passes; another author's group is genuinely refused. The MCP twin `aimeat_package_delete` costs `app:manage`… [triaged 2026-08-16]",
    "src/routes/packages.ts:DELETE:/v1/packages/:groupId/versions/:version": "DEBT medium, wants app:manage — Archives any single version of any package the owner authored, including the one installers currently resolve to. :739 tests the human's account name, so an app grant with one unrelated scope passes. [triaged 2026-08-16]",
    "src/routes/portal-api.ts:POST:/v1/portal/try-memory": "DEBT medium, wants memory:write — A full write into the owner's memory namespace. An app-grant token holding catalogue:read alone can post 280-char messages under the owner's identity and, because the write is an unconditional replace, can overwrite any existing `board.*` key of the owner and force it to visibility 'public' with a… [triaged 2026-08-16]",
    "src/routes/portal.ts:GET:/v1/portfolio/me": "OPEN BY DESIGN — A 302 to the owner's own public portfolio URL; it discloses only req.auth.owner, which the caller's token already carries, and the target page is unauthenticated. [triaged 2026-08-16]",
    "src/routes/portfolio.ts:GET:/v1/portfolio/catalog": "DEBT medium, wants memory:read — A complete inventory of the owner's content plus 120-char previews of every owner/public memory record. GET /v1/memory is gated on memory:read; this aggregate of the same content is reachable by an app grant holding any single scope, e.g. catalogue:read. [triaged 2026-08-16]",
    "src/routes/portfolio.ts:GET:/v1/portfolio/config": "DEBT low, wants memory:read — The owner's portfolio configuration record and the standalone portfolio URL, to any app grant with any scope. Same record as /v1/memory/portfolio.config, which requires memory:read. [triaged 2026-08-16]",
    "src/routes/portfolio.ts:PUT:/v1/portfolio/config": "DEBT high, wants memory:write — Full control of the owner's public portfolio configuration, including turning publication on or off and choosing what the page lists, on any single scope. The equivalent write through /v1/memory needs memory:write. [triaged 2026-08-16]",
    "src/routes/portfolio.ts:PUT:/v1/portfolio/upload": "DEBT high, wants storage:write — The right to publish arbitrary HTML as the owner's public portfolio page, served at /v1/portfolio/:username and <username>.portfolio.<apex>, on any single scope. This is the one route in the slice where the MCP surface asks for a word (storage:write) and the HTTP door does not — security DNA invari… [triaged 2026-08-16]",
    "src/routes/presence.ts:GET:/v1/presence/me": "DEBT low, wants social:read — The owner's presence settings (mode, manual status, who-can-see-me) and their true current status, including the 'invisible' state that every other viewer is denied. Any single scope reaches it. [triaged 2026-08-16]",
    "src/routes/presence.ts:PUT:/v1/presence/me": "DEBT medium, wants social:write — An app grant with any single scope rewrites the human's presence: it can set them 'invisible' or 'busy' to everyone, or flip visibility to 'everyone' so their live status is pushed to every active federation peer (services/presence.ts:232-233). The SSE ticket already refuses an app the power to spe… [triaged 2026-08-16]",
    "src/routes/presence.ts:GET:/v1/presence": "DEBT medium, wants social:read — Exactly what the owner's own session would see, and nothing more: the viewer identity is the owner GHII and every target's own visibility setting is applied per viewer inside the tracker. A batch of ids is capped at MAX_BATCH 200 (presence.ts:37, :88). [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/presence.ts:GET:/v1/presence/:ghii": "DEBT medium, wants social:read — One identity's status, viewer-scoped to the owner. A client-supplied GHII is not trusted: it selects a target, and the target's own visibility rule decides the answer. [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/prompts.ts:GET:/v1/agents/me/handbook/surface/:role": "OPEN BY DESIGN — Returns a compiled-in per-role handbook constant (services/handbooks/index.ts:28-30) that is identical for every caller; there is nothing behind this door for a scope to fence. [triaged 2026-08-16]",
    "src/routes/prompts.ts:GET:/v1/agents/me/handbook/offerings": "OPEN BY DESIGN — A compiled-in documentation constant with the caller's own name substituted; there is no record behind this door for a scope to protect. [triaged 2026-08-16]",
    "src/routes/prompts.ts:GET:/v1/agents/me/handbook/:module": "OPEN BY DESIGN — Node-managed operating documentation, personalised only with the caller's own name; the same prompt corpus is served with no auth at all at prompts.ts:132 (GET /v1/agents/me/handbook) and prompts.ts:463 (GET /v1/prompts/:tier), so a scope word would fence nothing. [triaged 2026-08-16]",
    "src/routes/prompts.ts:GET:/v1/prompts/draft-offer": "OPEN BY DESIGN — A compiled-in prompt constant with the caller's own identity substituted; the node hands out the text and never writes on it, and the publish it points to is gated on its own route. [triaged 2026-08-16]",
    "src/routes/prompts.ts:GET:/v1/prompts/agent-connect": "OPEN BY DESIGN — Copy-paste instructions built from config.baseUrl and the caller's own account name (prompts.ts:365); no credential is issued here and the device flow it describes still needs the human to approve the user_code, exactly as the unauthenticated sibling prompts.ts:335 does. [triaged 2026-08-16]",
    "src/routes/realtime.ts:POST:/v1/realtime/rooms": "DEBT medium, wants social:write — It can create rooms under the owner's identity with an attacker-chosen name and tags, published on the node's public room listing, and can consume the node-wide room ceiling until createRoom throws ROOM_LIMIT_REACHED (:72). Any single scope suffices. [triaged 2026-08-16]",
    "src/routes/realtime.ts:DELETE:/v1/realtime/rooms/:roomId": "GATED IN THE HANDLER OR THE SERVICE — Creator-or-operator check in the handler: realtime.ts:163-168 compares room.createdBy against req.auth!.sub before closing anything. [triaged 2026-08-16]",
    "src/routes/realtime.ts:GET:/v1/realtime/ice-servers": "DEBT low, wants social:read — The node's TURN username and credential, to any authenticated principal including an app grant holding one unrelated scope. No owner data is exposed, but a server-configured relay credential is: on a node that pays for TURN, that is the operator's bandwidth handed to anything with a token. The scop… [triaged 2026-08-16]",
    "src/routes/registration-invites.ts:POST:/v1/registration-invites": "OPEN BY DESIGN — The agent door: deliberately open so an AI with nothing but HTTP can invite one person. Per-IP and per-address limits, a mandatory model claim recorded in the message, and one uniform response so it cannot be used to probe addresses. [triaged 2026-08-16]",
    "src/routes/schedules.ts:GET:/v1/schedules": "DEBT low, wants workflow:read — The owner's entire automation inventory — every job row with its cron, its input (AI prompts, connection ids, task templates) and its target agent. Reachable on any single scope. [triaged 2026-08-16]",
    "src/routes/schedules.ts:GET:/v1/scheduler/tab": "DEBT low, wants workflow:read — The whole Scheduler tab payload: every managed schedule, every owner-installed extension cron, every agent's internal mirror, and the names and GAIIs of all the owner's agents. Any single scope. [triaged 2026-08-16]",
    "src/routes/schedules.ts:POST:/v1/schedules": "GATED IN THE HANDLER OR THE SERVICE — services/schedule-gate.ts:94-111 enforces the per-kind scope (ai:use / task:write / connections:use) and the cron; schedule-write.ts:291-293 and :301-307 add the extension-ownership and own-namespace refusals. [triaged 2026-08-16]",
    "src/routes/schedules.ts:GET:/v1/schedules/occurrences": "DEBT low, wants workflow:read — The owner's full firing calendar for up to ~62 days: which schedule ids fire when, and which run continuously. Any single scope. [triaged 2026-08-16]",
    "src/routes/schedules.ts:GET:/v1/schedules/:id": "GATED IN THE HANDLER OR THE SERVICE — Ownership comparison in the handler: schedules.ts:320 refuses (404) any job whose ownerScope is not the caller's owner GHII. [triaged 2026-08-16]",
    "src/routes/schedules.ts:PATCH:/v1/schedules/:id": "GATED IN THE HANDLER OR THE SERVICE — canManageSchedule in services/schedule-write.ts:186-190, applied at :403, plus the create-time input rules re-run on edit at :422-437. [triaged 2026-08-16]",
    "src/routes/schedules.ts:DELETE:/v1/schedules/:id": "GATED IN THE HANDLER OR THE SERVICE — canManageSchedule in services/schedule-write.ts:186-190, applied at :457 — the job must belong to the caller's owner, and a non-owner session may only manage agent-created schedules. [triaged 2026-08-16]",
    "src/routes/schedules.ts:POST:/v1/schedules/:id/trigger": "GATED IN THE HANDLER OR THE SERVICE — canManageSchedule in services/schedule-write.ts:186-190, applied at :487 — the job must be the caller's owner's, and a non-owner session may only run agent-created schedules. [triaged 2026-08-16]",
    "src/routes/schedules.ts:GET:/v1/agents/:name/schedules": "DEBT low, wants workflow:read — Every managed schedule targeting that agent plus the agent's internal scheduler mirror, on any single scope. APP_GRANTABLE_SCOPES already sells this as 'See your automations (workflows) and their runs' under workflow:read; this door does not ask for it. [triaged 2026-08-16]",
    "src/routes/schedules.ts:POST:/v1/agents/:name/schedules": "GATED IN THE HANDLER OR THE SERVICE — services/schedule-gate.ts:94-111 applies the per-kind scope (ai:use / task:write / connections:use) with no owner bypass for scoped principals, and schedule-write.ts:241 resolves the target agent under the caller's own owner. [triaged 2026-08-16]",
    "src/routes/schemas.ts:PUT:/v1/memory/:key/schema": "GATED IN THE HANDLER OR THE SERVICE — Owner-or-operator is decided in the handler (schemas.ts:28-32) and lock ownership again at :56, both before the write; role 'app', 'agent' and 'ecosystem' are all refused there. [triaged 2026-08-16]",
    "src/routes/schemas.ts:DELETE:/v1/memory/:key/schema": "DEBT medium, wants memory:write — For a schema set through PUT by a browser or PAT owner session (sub is the bare name 'alice') it gets 403. For every schema locked under the owner GHII — every workspace-provisioned strict lock — its own sub is that same string, so it passes :130 and deletes the lock plus the validation it enforced… [triaged 2026-08-16]",
    "src/routes/setup.ts:POST:/v1/setup/init": "GATED IN THE HANDLER OR THE SERVICE — First-run bootstrap, which by definition runs before any principal exists. Closed by the ALREADY_CONFIGURED guard at line 100 the moment the node has one owner, and by the setup IP allowlist at line 96 when one is configured. [triaged 2026-08-16]",
    "src/routes/sharing-groups.ts:GET:/v1/groups": "DEBT medium, wants memory:read — Every sharing group the owner is a MEMBER of, with each group's full member roster (identifier, identifierType, permissions) and default permissions — the owner's own groups are not in this branch, since only the isOwnerSession path calls listSharingGroups. So an app grant with one unrelated scope… [triaged 2026-08-16]",
    "src/routes/sharing-groups.ts:GET:/v1/groups/:id": "DEBT medium, wants memory:read — Full detail of any group the owner owns or belongs to. resolveIdentity returns the owner GHII for an app grant, so `group.ownerGaii === identity` at :120 is true for the owner's own groups and the member test covers the rest — the comparison refuses a different PERSON and admits an app acting in th… [triaged 2026-08-16]",
    "src/routes/skills.ts:GET:/v1/skills": "GATED IN THE HANDLER OR THE SERVICE — Every manifest returned passes the registry read gate the scanner cannot see: mayRead per record (services/skills.ts:256-267) and canReadWorkspace membership for workspace scope (:228-236); bodies are never loaded here. [triaged 2026-08-16]",
    "src/routes/skills.ts:POST:/v1/skills": "DEBT high, wants memory:write — It publishes or overwrites any user-scope skill in its owner's registry, in the owner's name and at any visibility including 'public', which the registry treats as federated (services/skills.ts:9) — holding whatever single scope the consent screen displayed. Republishing an existing name bumps the… [triaged 2026-08-16]",
    "src/routes/skills.ts:GET:/v1/skills/:name": "GATED IN THE HANDLER OR THE SERVICE — Resolution goes through resolveSkillRef, the registry's single access choke point: workspace membership at services/skills.ts:568 and the visibility gate at :593-595 decide every ref before any body is loaded. [triaged 2026-08-16]",
    "src/routes/skills.ts:GET:/v1/skills/:name/zip": "GATED IN THE HANDLER OR THE SERVICE — Same addressing and same choke point as the resolve route: the archive is assembled only from what resolveSkillRef returned, so the visibility and workspace-membership refusals (services/skills.ts:568, :593-595) decide it. [triaged 2026-08-16]",
    "src/routes/skills.ts:DELETE:/v1/skills/:name": "DEBT medium, wants memory:write — It deletes any user-scope skill in its owner's registry, including one published `visibility: 'public'`, which the registry treats as federated (services/skills.ts:9). Names are discoverable from GET /v1/skills, which the same token can read. Node scope is the only thing it cannot touch, because is… [triaged 2026-08-16]",
    "src/routes/skills.ts:GET:/v1/agents/:name/skills": "GATED IN THE HANDLER OR THE SERVICE — The owner comes from the token, never the path (skills.ts:304), an agent principal is confined to its own record (skills.ts:307-313), and every resolved ref still passes resolveSkillRef's visibility gate (services/skills.ts:593-595). [triaged 2026-08-16]",
    "src/routes/skills.ts:POST:/v1/agents/:name/skills": "DEBT high, wants memory:write — It attaches any ref its owner can resolve — including a stranger's public skill — to any agent under that owner, holding whatever single scope. A skill is an agent's operating guide, materialised into the runtime by the crewaimeat consumer (skills.ts:296), so this writes instructions into the owner… [triaged 2026-08-16]",
    "src/routes/skills.ts:DELETE:/v1/agents/:name/skills": "DEBT medium, wants memory:write — It strips any skill ref from any agent under its owner, holding whatever single scope was approved. The ref to remove comes from the body or query (:354) and the agent name from the path, so an app that can list the agents can empty their operating guides one call at a time. [triaged 2026-08-16]",
    "src/routes/skills.ts:GET:/v1/apps/:owner/:filename/skills": "GATED IN THE HANDLER OR THE SERVICE — The path only names a binding; every record returned passes the registry visibility gate per record (services/skills.ts:511 → mayRead :256-267), and only manifests are returned. [triaged 2026-08-16]",
    "src/routes/skills.ts:GET:/v1/agents/:name/skills/links": "DEBT low, wants memory:read — The full skill-link record of any agent under its owner — refs, names, descriptions, who linked them and when — on whatever single scope was approved. The read is fenced to the caller's own owner namespace, so no other account's data is reachable. [triaged 2026-08-16]",
    "src/routes/sse.ts:POST:/v1/events/ticket": "GATED IN THE HANDLER OR THE SERVICE — The ticket freezes the authorization: sse.ts:77-83 computes the allowed change domains from the principal's scopes (auth/sse-domain-scopes.ts:103-114), because the stream itself carries only the ticket. [triaged 2026-08-16]",
    "src/routes/templates.ts:POST:/v1/templates": "GATED IN THE HANDLER OR THE SERVICE — Operator-only in the handler: templates.ts:48 refuses any principal without the operator role while `packageCreateRole` is at its default 'operator' (config.ts:598); the route has no package-author check of its own, so if that setting is ever moved to 'owner' this needs a scope and an ownership tes… [triaged 2026-08-16]",
    "src/routes/templates.ts:PATCH:/v1/templates/:id": "DEBT medium, wants app:write — Rewrites the public gallery entry of any template published under the owner's name, including the screenshots array, and the text carries the owner's identity in the gallery. :342 compares the human's account name, so an app grant passes. [triaged 2026-08-16]",
    "src/routes/templates.ts:DELETE:/v1/templates/:id": "DEBT medium, wants app:manage — Deletes any gallery listing published under the owner's name, taking the template off the public gallery. `publishedBy` holds the bare owner name (set from `req.auth!.owner` at :91), so the comparison at :378 admits an app grant holding any single scope. [triaged 2026-08-16]",
    "src/routes/templates.ts:POST:/v1/templates/:id/review": "DEBT medium, wants social:write — Writes or overwrites a 1-to-5 rating and comment on any listing in the gallery, under `authorName` = the human's account name (:438), and moves the listing's public average. An app grant needs only to be authenticated; its `sub` is the owner's GHII, so its row is a separate one from the owner's own… [triaged 2026-08-16]",
    "src/routes/templates.ts:POST:/v1/templates/:id/discussion": "DEBT medium, wants social:write — Posts public messages on any template listing on the node, displayed under the human's account name. GET /v1/templates/:id (:303) serves those discussions to unauthenticated readers, so an app grant holding one unrelated scope publishes text in the owner's name on a public surface. [triaged 2026-08-16]",
    "src/routes/unfurl.ts:GET:/v1/unfurl": "OPEN BY DESIGN — Reads no owner data and writes nothing — it returns public OpenGraph metadata for a caller-supplied URL, bounded by safeFetch's SSRF filter, a 512 KB cap, an 8s timeout and a 1h cache. [triaged 2026-08-16]",
    "src/routes/unfurl.ts:GET:/v1/unfurl/image": "OPEN BY DESIGN — A same-origin proxy for public image bytes (the SPA's CSP forbids remote img hosts); no owner data, bounded by safeFetch, an image/* content-type check, a 4 MB cap and nosniff. [triaged 2026-08-16]",
    "src/routes/upload.ts:PUT:/v1/upload/:token": "GATED IN THE HANDLER OR THE SERVICE — The presigned token IS the credential: verifyUploadToken (upload.ts:125) checks the node-key signature, expiry and single use, and subject, actor, type and size ceiling are all read from the verified token rather than the request. [triaged 2026-08-16]",
    "src/routes/usage.ts:GET:/v1/owner/usage": "DEBT medium, wants wallet:read — The owner's morsel balance and every quota figure. Same balance number that GET /v1/wallet gates on wallet:read (wallet.ts:51) — here an app grant reads it holding whatever single scope its consent screen showed. [triaged 2026-08-16]",
    "src/routes/usage.ts:GET:/v1/owner/home": "DEBT medium, wants wallet:read — The owner's morsel balance, every quota bar and the whole agent fleet, in one call, on requireAuth alone. An app approved only for, say, memory:read enumerates the owner's agents and reads the balance that wallet:read exists to fence. [triaged 2026-08-16]",
    "src/routes/validate.ts:POST:/v1/validate": "OPEN BY DESIGN — A stateless dry-run: it parses a caller-supplied body against a static schema map and touches no storage and no identity, so there is nothing to authorize; the global rate limiter (server.ts:153) bounds the cost. [triaged 2026-08-16]",
    "src/routes/verification.ts:GET:/v1/ghii/verify/eudiw/request": "DEBT high, wants account:security — The opening half of an identity-verification rail bound to the human: it returns a `state` for a nonce stamped with the owner's name, and the unauthenticated callback then writes verificationLevel 3 and the presented credential's issuer and attributes onto the human's GHII. No scope word is require… [triaged 2026-08-16]",
    "src/routes/verification.ts:POST:/v1/ghii/verify/eudiw/callback": "OPEN BY DESIGN — An OpenID4VP wallet callback, which by definition arrives with no session. The state must name a live single-use nonce, the presentation is verified against the trusted issuers, and the person written to comes from that nonce and never from the request body. [triaged 2026-08-16]",
    "src/routes/verification.ts:GET:/v1/ghii/verify/ftn/authorize": "DEBT high, wants account:security — The opening half of the FTN rail bound to the human: an authorization URL and a state for a nonce stamped with the owner's name. Whoever completes the bank-ID login against that state has their own verified attributes written onto the human's GHII at level 3. No scope word is required. [triaged 2026-08-16]",
    "src/routes/verification.ts:GET:/v1/ghii/:ghii/credential": "DEBT high, wants requireOwnerPrincipal() (account:security), matching the two sibling write doors in the same file — A signed W3C identity credential for the human, with no scope word. This is invariant 11 exactly: req.auth.owner holds the person's account name on app grants, agent JWTs and GEAI tokens alike, so `ownerName !== req.auth.owner` names a PERSON and admits everything acting in that person's name. The… [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/verification.ts:GET:/v1/consent/:id/receipt": "DEBT medium, wants consent:manage — Its own owner's consent receipts, with no scope word — the check resolves the caller's owner name to that owner's GHII and agent GAIIs, so every principal acting in the person's name passes. Another owner's receipt is refused at 412, which is the boundary that matters here. [triaged 2026-08-16, reclassified by the skeptic pass]",
    "src/routes/verification.ts:GET:/v1/trusted-issuers": "DEBT low, wants catalogue:read — The node's full trusted-issuer list with no scope word. Nothing owner-specific is behind the door and public keys are not secrets, which is why this rates low rather than high; the honest alternative to a scope is to drop requireAuth and let it be genuinely public, since it is the sort of trust-anc… [triaged 2026-08-16]",
    "src/routes/wallet.ts:GET:/v1/wallet/history": "DEBT high, wants wallet:read — The owner's full transaction history, counterparties and tracking codes included — the exact data wallet:read was written to fence, handed to an app grant holding any other word, through the deprecated twin of the gated door. Deleting the route at its declared Sunset closes it as cleanly as adding… [triaged 2026-08-16]",
    "src/routes/wallet.ts:POST:/v1/wallet/request": "DEBT medium, wants wallet:read — It can claim the owner's allowance and write rows into their money ledger. The ceiling in creditBalanceCapped bounds it — nothing can be drained, and a balance already at the cap returns 409 — so the exposure is an unattributed write to the wallet rather than a loss. Note for the fix: this repo has… [triaged 2026-08-16]",
    "src/routes/webmcp.ts:POST:/v1/apps/:owner/:filename/webmcp/tools/:tool": "GATED IN THE HANDLER OR THE SERVICE — Two decisions the scanner cannot see: authoriseMeteredCall (webmcp.ts:185-189) refuses an app grant without contract:spend and enforces its spend cap (services/metered-access.ts:217-229), and the unpriced branch runs requireAuth() inline at :253 while a priced tool with no right always answers 402. [triaged 2026-08-16]",
    "src/routes/auth-otk.ts:POST:/v1/auth/initial-otk": "DEBT high, wants memory:write — A bearer-less, shareable write credential over the owner's namespace. An app-grant token's sub is the OWNER's GHII (src/routes/app-grants.ts:522), so an app approved for memory:read alone mints a key whose redemption at /v1/mm carries no credential at all and writes as the owner, including flipping… [triaged 2026-08-16]"
  }
}
