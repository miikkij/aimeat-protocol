```html
<!DOCTYPE html>
<!-- AIMEAT App Manifest
name: prh-yritystietopalvelu
version: 1.0.0
description: PRH Yritystietopalvelu - Suomalaisten yritysten tietopalvelu
author: generator
-->
<html lang="fi">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>PRH Yritystietopalvelu</title>
  <style>
    /* Minimal app shell styles */
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: system-ui, -apple-system, sans-serif; background: #f7fafc; color: #1a365d; }
    .app-loading { display: flex; justify-content: center; align-items: center; min-height: 100vh; font-size: 1.1rem; color: #4a5568; }
    .app-error { max-width: 600px; margin: 2rem auto; padding: 1.5rem; background: #fff5f5; border: 1px solid #fc8181; border-radius: 8px; color: #c53030; }
    .app-header { background: #1a365d; color: white; padding: 0.75rem 1.5rem; display: flex; align-items: center; justify-content: space-between; }
    .app-header h1 { font-size: 1.1rem; font-weight: 600; }
    .app-header-right { display: flex; align-items: center; gap: 1rem; font-size: 0.85rem; }
    .app-container { max-width: 1100px; margin: 0 auto; padding: 1rem; }
  </style>
</head>
<body>
  <div id="app" class="app-loading">Ladataan...</div>

  <!-- AIMEAT Platform Libraries -->
  <script src="/v1/libs/aimeat-auth.js"></script>
  <script src="/v1/libs/aimeat-data.js"></script>

  <!-- Cortex Libraries (load in dependency order) -->
  <script src="/v1/cortex/prh-yritysdata/libs/prh-yritysdata.js"></script>
  <script src="/v1/cortex/prh-haku/libs/prh-haku.js"></script>
  <script src="/v1/cortex/prh-seurantalista/libs/prh-seurantalista.js"></script>
  <script src="/v1/cortex/prh-vertailu/libs/prh-vertailu.js"></script>
  <script src="/v1/cortex/prh-sovellusalue/libs/prh-sovellusalue.js"></script>

  <script>
  (async function() {
    var app = document.getElementById('app');
    try {
      // Initialize app-domain cortex (handles auth + translations)
      var result = await AIMEAT.prhSovellusalue.init();
      if (!result || !result.authenticated) {
        app.className = '';
        app.innerHTML = '';
        // Show header even when not authenticated
        var header = document.createElement('div');
        header.className = 'app-header';
        header.innerHTML = '<h1>PRH Yritystietopalvelu</h1>';
        app.appendChild(header);
        var loginContainer = document.createElement('div');
        loginContainer.className = 'app-container';
        loginContainer.style.marginTop = '2rem';
        loginContainer.style.textAlign = 'center';
        app.appendChild(loginContainer);
        AIMEAT.auth.mountLoginButton(loginContainer);
        return;
      }
      // Render the full application
      app.className = '';
      app.innerHTML = '';
      AIMEAT.prhSovellusalue.render(app);
    } catch (e) {
      app.className = 'app-error';
      app.textContent = 'Sovelluksen käynnistys epäonnistui: ' + (e.message || 'Tuntematon virhe');
    }
  })();
  </script>
</body>
</html>
```
