/**
 * @file src/data/library-packs/sdk-living.ts
 * @author Jouni Miikki
 * SPDX-License-Identifier: MIT
 * @description The aimeat-living pack entry, spread into SDK_PACKS by ./sdk.ts — the living
 *   document: ONE JSON record whose parts are wired to each other through formulas in a single
 *   dependency graph, so touching one thing moves everything that stood on it.
 *
 *   IT LIVES IN ITS OWN FILE for the reason vendored.ts and sdk-ui.ts already do: an aiDoc that
 *   teaches a whole record format is the longest text in the registry, and sdk.ts is under the
 *   800-line rule. This file replaces the sdk-science.ts placeholder that was left behind when
 *   c932f2f17 was reverted; nothing imports SCIENCE_PACKS any more.
 * @structure LIVING_PACKS: LibraryPack[]
 * @usage Imported by ./sdk.ts (registry assembly). Do not import directly.
 * @version-history
 *   v1.3.0 — 2026-09-06 — aimeat-living 0.5.0: the aiDoc gains "rows are arrays", because a
 *     document that can hold a list is a different thing to write than one that cannot, and a
 *     model that does not know range() and scan() exist will write twenty-four nodes for a day.
 *   v1.2.0 — 2026-09-06 — aimeat-living 0.4.0: a label is a language map, so the aiDoc teaches
 *     writing the record in every language it is meant to be read in.
 *   v1.1.0 — 2026-09-05 — aimeat-living 0.2.0: the three copies of kit behaviour dropped now that
 *     atelier 0.53.0 carries them.
 *   v1.0.0 — 2026-09-05 — Initial: aimeat-living 0.1.0 (the living document, stage 1).
 */
import type { LibraryPack } from './types.js';

export const LIVING_PACKS: LibraryPack[] = [
  {
    id: 'aimeat-living',
    kind: 'sdk',
    category: 'ui',
    title: 'Living document',
    description: 'The living document: ONE JSON record — an arrangement plus a dependency graph of values, formulas with units, controls, bindings, sentences, a statechart and live sources — mounted as a screen where touching one thing moves everything that stood on it. The formulas are spreadsheet formulas, set as real mathematics; the sentences change with the state; the chain of who feeds whom is drawable, and flashes along the path a change travelled. The AI writes the record and the person edits it with their own AI or touches the controls: there is no app code in between.',
    url: '/v1/libs/aimeat-living.js',
    include: [
      '<link rel="stylesheet" href="{{BASE_URL}}/lib/aimeat-atelier.css">',
      '<link rel="stylesheet" href="{{BASE_URL}}/lib/aimeat-living.css">',
      '<script src="{{BASE_URL}}/v1/libs/aimeat-atelier.js"></script>',
      '<script src="{{BASE_URL}}/v1/libs/aimeat-living.js"></script>',
    ],
    requires: ['aimeat-atelier'],
    license: 'MIT',
    apiSurface: 'AIMEAT.living',
    aiDoc: [
      'A LIVING DOCUMENT IS ONE MEMORY RECORD THAT YOU WRITE, not an app you build. The person keeps',
      'it under a memory key; you write the record; AIMEAT.living.mount(el, record) turns it into a',
      'screen. There is no app code between the record and what is on the screen, so an edit to the',
      'record IS an edit to the document, and the person can ask their own AI for that edit.',
      '',
      'THE RECORD: { v: 1, register, look, layout, model }',
      '  layout — an ordinary mosaic arrangement: { v: 1, nav, blocks: [{ id, component, span, props }] }.',
      '           Exactly what you already write for an Atelier app, and the person can rearrange it.',
      '           `span` is a WORD on the six-column composition grid — "full" (the whole line, and the',
      '           default), "main" (four), "side" (two), "half" (three) — never a number. A number is not',
      '           a span the stylesheet knows, and every block then lands in one narrow column.',
      '  model  — { nodes: { <id>: { type, … } } }: ONE dependency graph. Every node has an id you',
      '           choose, a type, and the fields that type takes. Nothing else.',
      '',
      'THE EIGHT NODE TYPES. Ask the library rather than trusting this list: AIMEAT.living.describe()',
      'returns every type id and describe("trigger") returns its inputs, outputs, options and a worked',
      'example — generated from the source, so it is never stale.',
      '  value    a named quantity, the writable ground: { type:"value", value, unit?, min?, max?, step?, label? }',
      '  formula  a spreadsheet expression over other node ids: { type:"formula", expr, unit?, label?, block? }',
      '  control  a slider|toggle|pick|number|text bound to ONE value: { type:"control", kind, target, label?, options?, block? }',
      '  binding  one block prop reads one node: { type:"binding", block, prop, from }  (prop "." hands the whole record over)',
      '  text     a sentence that changes with the graph: { type:"text", template, block? }',
      '  machine  a statechart in XState words: { type:"machine", initial, states, when?, block? }',
      '  source   a live value from a memory key or a url: { type:"source", key|url, path?, raw?, every?, unit?, value }',
      '  trigger  when a machine moves, tell somebody: { type:"trigger", on, target, enabled?, include?, label? }',
      '',
      'FORMULAS ARE SPREADSHEET FORMULAS. p * v / (r * T) · if(t > 30, "liian kuuma", "hyvä") · avg(readings)',
      '· clamp(x, 0, 1) · min max abs sqrt pow log exp round floor ceil · sum avg min max count first last over a',
      'list · and or not · = <> < <= > >= · & joins text · convert(x, "K"). A name in a formula is another',
      'NODE ID. A formula also answers with its own TeX: read it as <id>.tex.',
      '',
      'UNITS ARE CARRIED AND CHECKED. A unit rides through multiplication and division and is checked on',
      'addition: adding °C to Pa is refused in words rather than silently computed. SI plus the common ones,',
      'compound units written out ("J/(mol*K)", "km/h", "m/s^2"), % and kWh, and a currency code (EUR, USD)',
      'as its own family that never converts. ONE RULE TO KNOW: °C and °F are scales with an OFFSET, so',
      'multiplying one has no meaning as a temperature and the answer comes back as a plain number — which',
      'is why { expr: "t * 9/5 + 32", unit: "°F" } means what its author meant. A real conversion is asked',
      'for: convert(t, "K"), or a unit on a formula whose result still carries one.',
      '',
      'WHERE A NODE APPEARS ON THE SCREEN, two roads and no third:',
      '  a BINDING feeds a kit component — a gauge\'s value, a figure\'s value, a chart\'s labels and series,',
      '  a statRow\'s tiles. The refresh goes through the mosaic\'s own door, so the kit\'s motion runs: the',
      '  figure counts to its new number and the chart\'s marks move. You write nothing for that.',
      '  a BLOCK FIELD on a control, formula, text, machine or value node draws it into a `section` block',
      '  (component: "section") through the mosaic\'s fill. It must be a section; anything else is refused.',
      '',
      'MOUNTING: const d = AIMEAT.living.mount(host, record, { onChange(e) { … }, chainBlock: "chain" }).',
      'd.set(id, value) moves a node the way a control does — the same door for a person and for an agent.',
      'd.get(id) · d.values() · d.state() · d.send("EVENT") · d.chain(el) draws the dependency graph and',
      'flashes the path a change travelled. chainBlock names a section to draw that chain into.',
      'AIMEAT.living.validate(record) reads a document WITHOUT running it and returns every refusal in words',
      '(an unknown node, a circle naming the two ids, a unit that will not add, a block that is not there).',
      'Call it before you save; a refused document says so on the screen instead of rendering blank.',
      '',
      'A CIRCLE IS REFUSED BY NAME. If a needs b and b needs a, the document does not mount and says which',
      'two. Do not try to break it with an extra node: the graph is meant to be acyclic and a circle is a',
      'mistake in the model, not a limitation.',
      '',
      'IT COMPUTES IN THE BROWSER — no route, no round trip. KaTeX is fetched from this node only when a',
      'formula is actually printed, and the answer is written as text first, so a page where it never loads',
      'still says what the formula worked out to.',
      '',
      'THREE WORKED EXAMPLES.',
      '1) A reading a person moves, converted, judged and explained:',
      '   { "t": { "type":"value", "value":22, "unit":"°C", "min":-20, "max":45, "step":0.5, "label":"Lämpötila" },',
      '     "slider": { "type":"control", "kind":"slider", "target":"t", "block":"controls" },',
      '     "f": { "type":"formula", "expr":"t * 9/5 + 32", "unit":"°F", "label":"Fahrenheit", "block":"maths" },',
      '     "dial": { "type":"binding", "block":"gaugeBlock", "prop":"value", "from":"t" },',
      '     "note": { "type":"text", "block":"note",',
      '               "template":"Nyt on {{ t | 1 }} °C, {{ if t > 30 }}liian kuuma{{ else }}hyvä{{ end }}." } }',
      '2) A law with real units, checked all the way through:',
      '   { "p": { "type":"value", "value":101325, "unit":"Pa" },',
      '     "v": { "type":"value", "value":0.0224, "unit":"m^3" },',
      '     "r": { "type":"value", "value":8.314462618, "unit":"J/(mol*K)" },',
      '     "T": { "type":"value", "value":273.15, "unit":"K" },',
      '     "n": { "type":"formula", "expr":"p * v / (r * T)", "unit":"mol", "label":"Ainemäärä", "block":"maths" } }',
      '3) A statechart that changes what the document says as the reading crosses:',
      '   { "state": { "type":"machine", "block":"stateBlock", "initial":"fine",',
      '       "states": { "cold": { "on": { "WARM":"fine" } },',
      '                   "fine": { "on": { "HOT":"hot", "COLD":"cold" } },',
      '                   "hot":  { "entry": { "advice":"\\"tuuleta\\"" },',
      '                             "on": { "COOL": { "target":"fine", "guard":"t < 30" } } } },',
      '       "when": [ { "expr":"t > 30", "send":"HOT" }, { "expr":"t < 30", "send":"COOL" },',
      '                 { "expr":"t < 5", "send":"COLD" } ] },',
      '     "advice": { "type":"value", "value":"", "label":"Ohje" } }',
      '   Entry and exit assign to value nodes; guards and crossings are ordinary formulas; after: { 5000:',
      '   "fine" } is a timer. The machine\'s output is its state as a dotted path, so if(state = "hot", …)',
      '   works from any other node.',
      '',
      'A LABEL IS A LANGUAGE MAP. Any human-facing string in the record may be written as',
      '{ "fi": "Ilma ovella", "en": "Air at the door" } instead of a plain string — a label, a hint, a',
      'pick\'s option label, a text node\'s whole template, the words a machine\'s entry action writes, and a',
      'layout block\'s title, sub or caption. The page\'s language decides which is read (the login pill sets',
      'it), the record\'s own top-level "lang" is the fallback, and the map\'s first key is the last resort, so',
      'the document always says something. `"langs": ["fi","en"]` is the optional list of what it carries.',
      'Ask AIMEAT.living.describe("control").languages for the fields a given type takes as a map. WRITE EACH',
      'LANGUAGE AS ITSELF rather than translating one into the other, and give a sentence THE SAME {{ }} holes',
      'in every language: validate() refuses a template that reads a node in one language and not the other,',
      'and refuses a map with no language keys in it, naming both. A `format` is per record rather than per',
      'language — decimals and unit placement are facts about the measurement — except `locale: "auto"`,',
      'which writes the separators in whatever language the page is reading. Changing language moves the words',
      'only: values stay where the person left them and the machine stays in the state it reached.',
      '',
      'ROWS ARE ARRAYS, and this is the half of a spreadsheet that makes a document worth writing. A node\'s',
      'value may be a LIST, and every arithmetic and comparison goes down it element by element: `pv - load`',
      'is a row of differences, `max(0, pv - load)` a row of surpluses, `sum(load * price)` the day\'s bill.',
      'A plain number repeats against a list; two lists of different lengths are refused with both lengths; a',
      'unit that will not add at element 7 is refused saying 7. Build a row with range(24) — counted from 0,',
      'stopping BEFORE the last — and with map(xs, expr), where the element is `x` and its position `i`. Walk',
      'one with fold(xs, start, expr) or scan(xs, start, expr), where what is being built is `acc`; scan',
      'answers with EVERY accumulator INCLUDING the one it started from, so 24 hours in gives 25 readings —',
      'the state of charge at each hour BOUNDARY, and the flow during hour i is position i+1 less position i.',
      'Read a row back with index(xs, i) (counted from 0), at(xs, t) (which reads BETWEEN two positions and',
      'stops at the ends, so a clock scrubbed to 13:30 reads an hourly curve), cumsum(xs) and where(cond, a, b),',
      'the element-wise if. min(xs) and max(xs) still REDUCE a row to one value; min(a, b) and max(a, b) with',
      'two or more arguments go element by element. The trigonometry is there too — sin cos tan asin acos atan',
      'atan2 log10 and pi — and every angle is in RADIANS, with deg() and rad() as the two doors.',
      'So a 24-hour day is ONE node, not twenty-four; a battery hour by hour is one scan; a year of irradiation',
      'on a tilted plane is one 288-element vector. Ask describe("formula").functions for the whole list.',
      '',
      'A DOCUMENT CAN REACH OUT, AND LISTEN. A `trigger` fires when a machine TRANSITIONS — not on every',
      'recompute, so a slider dragged across a threshold sends ONE message — and that message carries the',
      'whole state: { document: { key, title, register }, at, transition: { node, from, to, event },',
      'values: { <id>: { value, unit, label } }, machines: { <id>: state }, trigger: { id, label } }. The',
      'labels are read in the page\'s language; the ids never change. A row travels as its length and a head',
      'of 24 unless `include` names that node, in which case it goes whole. `target` is either',
      '{ kind:"url", url, method } or { kind:"agent", agent }, and an agent target becomes a task for that',
      'agent titled "Living document: <title>, <from> → <to>".',
      'A `source` reads the other way: { type:"source", url, path:"prices[0].price", every:900 } asks that',
      'address every `every` seconds while the document is open and the tab is visible — the floor is 10 —',
      'and `raw:true` takes the body itself. A read that FAILS keeps the last value and writes the refusal',
      'into the node\'s `stale` output, which a sentence can print as {{ spot.stale }}: a price that dropped',
      'to zero because a server was down for one poll would be a document that lies with a number.',
      'BOTH ROADS GO THROUGH THE NODE\'S `living-hooks` EXTENSION as the signed-in caller. The browser never',
      'calls a third-party address itself, the owner\'s allowlist decides which hosts may be reached, and a',
      'guest sees both roads disabled with words rather than a page that quietly does nothing. Two switches',
      'are the owner\'s and both live in the record: `hooks: { enabled: false }` on the document stops every',
      'trigger at once, and `enabled` on a trigger stops that one.',
      'ON THE SCREEN, each value, control and source carries a small gear with an arrow going IN, and each',
      'machine and trigger a gear with the arrow going OUT. The dialogs behind them are generated from the',
      'record: the exact answer a URL must give for THIS node\'s path, the POST that writes the value into a',
      'memory key of the document\'s own (<document key>.in.<node>), the sentence to say to your own AI, and',
      'the payload exactly as it would leave. Pass { gears: false } to mount() to leave them off.',
      'The mounted handle: d.hooks() says whether this page can tell anybody anything, d.deliveries() gives',
      'the last fifty of this mount, d.test(triggerId) sends a sample marked test:true, d.onDelivery(cb)',
      'hears each one, and d.onRecordChange(cb) hears a gear editing the record so the app can save it —',
      'this library persists nothing.',
      '',
      'TEMPLATES: {{ node }} prints the NUMBER (the sentence around it carries the unit), {{ node | unit }}',
      'prints both, {{ node | 1 }} fixes the decimals, and int, percent, upper and lower are the rest.',
      '{{ if expr }}…{{ else }}…{{ end }} takes any expression. The output is TEXT, never markup.',
      '',
      'DO NOT write your own recompute loop, your own formula parser, your own animation or your own',
      'unit table. Move a node and the library does the rest, recomputing only what stood on it and',
      'reporting exactly which nodes changed.',
    ].join('\n'),
    changelog: [
      { version: '0.6.0', date: '2026-09-06', summary: 'The document reaches out, and listens. A `trigger` node fires when a machine TRANSITIONS — not on every recompute — and sends one message carrying the whole state: which document, the time, which machine moved from where to where on which event, every value with its unit and its label in the page\'s language, and every machine\'s state. Its target is an address or one of the owner\'s own agents, and an agent target becomes a task titled with the crossing; a row travels as its length and a head of 24 unless `include` names it. A `source` gained the other road in: a url, a path with brackets, a raw body and an `every` in seconds with a floor of ten, where a read that fails KEEPS the last value and writes the refusal into the node\'s new `stale` output, because a price that dropped to zero for one poll would be a document that lies with a number. Both roads go through the node\'s living-hooks extension as the signed-in caller, so the owner\'s allowlist decides which hosts may be reached and a guest is told in words rather than watching a page do nothing. Every value, control and source now carries a gear with an arrow going in and every machine and trigger one with the arrow going out; the dialogs behind them are generated from the record and from describe(), so what is on the screen is the answer this exact node needs rather than a plausible example. Two switches, both the owner\'s and both in the record: `hooks: { enabled }` on the document and `enabled` on each trigger.' },
      { version: '0.5.0', date: '2026-09-06', summary: 'A row is a value, which is the half of a spreadsheet this language did not have. A node may hold a LIST, and every arithmetic and comparison goes down it element by element — `max(0, pv - load)` is a row of surpluses and `sum(load * price)` is the day\'s bill — with a plain number repeating against a list, two lists of different lengths refused with both lengths, and a unit that will not add at element 7 refused saying 7. range(n) builds a row, map(xs, expr) walks one with the element as `x` and its position as `i`, fold and scan walk one carrying `acc`, and scan answers with EVERY accumulator including the one it started from, so 24 hours in gives 25 readings and the flow during an hour is the difference between two of them. index(xs, i) reads a whole position counted from 0, at(xs, t) reads between two and stops at the ends, cumsum and where complete the set, and min/max reduce on one argument and go element-wise on more. The trigonometry and pi arrive with them, in radians, with deg() and rad() as the two doors. A day of twenty-four hours, a battery simulated hour by hour and a year of irradiation on a tilted plane are three formulas in a record now instead of three hundred nodes. describe("formula").functions answers with the whole vocabulary, generated from the source.' },
      { version: '0.4.0', date: '2026-09-06', summary: 'A record carries its own words. Any human-facing string may be a language map — { "fi": "Ilma ovella", "en": "Air at the door" } — on a label, a hint, a pick\'s option, a sentence template, the words a machine\'s entry action writes, or a layout block\'s title and sub, so one record is the document in every language it was written for instead of one record per language. The page\'s language decides which is read, the record\'s own "lang" is the fallback, and the map\'s first key is the last resort. A change of language moves the WORDS ONLY: the value a person moved is where they left it, the machine is in the state it reached, nothing is remounted and no entrance runs again. validate() refuses a map with no language in it and a sentence whose holes differ between languages, naming the node and the language; describe(type).languages says which fields take a map; and a `format` stays per record except `locale: "auto"`, which writes the separators in the page\'s language.' },
      { version: '0.2.0', date: '2026-09-05', summary: 'Nothing to change in a record: this release is three pieces of behaviour handed back to the Atelier kit, which now carries them (0.53.0). A control row is one of the kit\'s own form fields, so a slider gets the kit\'s track, its reading, its label wiring and a 40px hit area rather than markup this library built beside it. The chain view stops padding its columns to keep a long label inside the graph\'s frame; the graph keeps its own pills inside now, so an outer node reads whole at any width. And which layout components accept a bound record is asked of the mounted arrangement instead of being a copied list here — so a binding aimed at a block that cannot read one is refused in the kit\'s words on the mounted handle (`refusals`), where validate() alone could only guess.' },
      { version: '0.1.0', date: '2026-09-05', summary: 'Initial. One JSON record — { v, register, look, layout, model } — mounted as a living screen: seven node types (value, formula, control, binding, text, machine, source) in one dependency graph, spreadsheet formulas with units carried through multiplication and checked on addition, a TeX printer riding the same tree as the evaluator, an XState-vocabulary statechart with guards, entry and exit assignments, timers and crossings, bindings that refresh through the mosaic so the kit\'s own motion runs, templates that change with the state, live sources on the platform\'s own change event, describe() generated from the node modules\' JSDoc, and a chain view that flashes the path a change travelled.' },
    ],
    tierHint: 'T1',
    // Its own track, like aimeat-atelier: no promptGroup, no promptLine, no interview triggers —
    // a living document is written as a RECORD, and the Classic build prompt must not teach it.
    interviewTriggers: [],
    sizeEstimate: '~30KB (KaTeX is fetched only when a formula is printed)',
    status: 'preview',
    modelTier: 'needs-doc',
  },
];
