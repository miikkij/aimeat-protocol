{
  "name": "aimeat-desktop",
  "version": "0.1.0",
  "description": "AIMEAT Personal Node — Desktop GUI (Tauri)",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "pnpm stage && tauri build",
    "package": "pnpm stage && tauri build",
    "stage": "node scripts/stage-node.mjs && node scripts/stage-server.mjs",
    "stage:node": "node scripts/stage-node.mjs",
    "stage:server": "node scripts/stage-server.mjs",
    "tauri": "tauri"
  },
  "dependencies": {},
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
