# What the jeff image installs: jeff's own dependencies (its pyproject.toml at the pinned commit),
# plus ONNX for its CPU arm. jeff itself runs from its source on PYTHONPATH.
# lock.sh turns this into hash-pinned requirements-cpu.txt and -cu128.txt.
torch
gliformer>=0.1.1
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27
huggingface-hub>=0.24
modal>=1.5
onnx>=1.16
onnxruntime>=1.18
