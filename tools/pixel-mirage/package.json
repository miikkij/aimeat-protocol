{
  "name": "pixel-mirage",
  "private": true,
  "//": "The repo root is ESM. engine.js and codec.js are plain scripts: the browser loads them via <script> and the QuickJS sandbox has no module system at all. Declaring commonjs here keeps `require()` working in the test harnesses without giving the shared sources a module system they cannot have.",
  "type": "commonjs",
  "scripts": {
    "build": "node build.mjs",
    "deploy:ext": "node build.mjs && node deploy-ext.mjs",
    "test": "node test/codec.test.mjs && node test/engine.test.mjs fixtures/live/p1600.jpg && node test/action.test.mjs fixtures/live/p1600.jpg"
  }
}
